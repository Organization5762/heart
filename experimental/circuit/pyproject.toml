[project]
name = "circuit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.2"
dependencies = [
    "kipart",
    "kicost",
    "kicad-python",
    "skidl",
]