repos:
  - repo: local
    hooks:
      - id: make-format
        name: Run make format
        entry: bash -c 'export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"; make format'
        language: system
        pass_filenames: false
