[project]
name = "heart"
version = "0.1.0"
keywords = []
authors = []

dependencies = ["pygame", "openant"]

[build-system]
requires = [
    "setuptools",
    "setuptools-scm"
]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
heart = ["*.png"]