[project]
name = "heart"
version = "0.1.0"
keywords = []
authors = []
requires-python = ">= 3.10"

dependencies = ["pygame", "openant", "Pillow"]

[build-system]
requires = [
    "setuptools",
    "setuptools-scm"
]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
heart = ["*.png"]

[project.scripts]
totem = "heart.projects.loop:run"