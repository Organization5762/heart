# Heart Repository Agent Instructions

## Installing

Use the tooling in this repository to manage environments. Prefer `uv` for Python dependency resolution.

- On Pi 5, install the `heart_pi5_scan_loop` kernel module through DKMS via `packages/heart-device-manager` instead of ad-hoc `insmod` so kernel updates rebuild it automatically.
- Pin prerelease Python dependencies with exact versions in `pyproject.toml` so `uv.lock` resolves the intended alpha or beta release.
- Scene and asset bootstrap can run before `pygame.display.set_mode`; avoid unconditional `convert()` or `convert_alpha()` calls in asset constructors and defer display-dependent conversion until a surface exists.
- `DisplayContext` wraps the active `pygame.Surface`; renderers that need surface-only APIs such as `subsurface()` must use `window.screen` after confirming it is initialized instead of calling those APIs on `DisplayContext` directly.
- Only the real display-owned `DisplayContext` may change pygame display modes; scratch or post-processing contexts must keep `can_configure_display=False` and never call `pygame.display.set_mode()`.
- OpenGL-backed renderers must treat `reset()` as lifecycle teardown: cascade reset into nested runtimes and restore mutated UI state such as mouse visibility so mode switches can leave GPU-backed scenes cleanly.
- Pi 5 resident-scan backends own steady-state refresh in hardware; the generic Rust runtime worker should wait for new work instead of re-submitting an unchanged active frame in software.
- When benchmarking Pi-side Rust binaries remotely, use a release build (`cargo run --release ...` or a freshly rebuilt `cargo build --release ...` binary) and treat `cargo check` output as compile-only validation, not performance evidence. Prefer `cargo run --release --manifest-path ...` over direct `target/release/...` invocations unless you just rebuilt that exact binary, because stale per-crate outputs can linger under `rust/heart_rgb_matrix_driver/target` and silently skew measurements.
- On Pi 5 dense-frame changed-frame tuning, keep the default Rayon-backed scan packer unless fresh Pi benchmarks show otherwise; forcing `HEART_PI5_SCAN_PACK_PARALLEL_THRESHOLD_WORDS` high or reducing `RAYON_NUM_THREADS` below the default has consistently hurt `distinct_frame_update_hz`.
- Pi 5 scanout v1 only supports `WiringProfile::AdafruitHatPwm`; reject `AdafruitHat` because the packed transport rebases GPIO words to `5..27` and the resident/kernel transport hard-codes `OE` on GPIO `18`.
- Pi 5 resident-loop `STOP` must not return until the worker has released its replay snapshot of `frame_cpu`; otherwise an immediate `LOAD` can race in-flight MMIO replay and tear the resident frame buffer.
- Pi firmware defaults managed by `packages/heart-device-manager/src/heart_device_manager/install_rgb_matrix.sh` should be applied idempotently; keep `dtparam=audio=off`, `pcie_aspm=off`, and the current CPU-isolation flag in that installer instead of relying on ad-hoc manual edits.

## Formatting

Run `make format` before committing changes. This applies Ruff fixes, isort, Black, docformatter, and mdformat to Python sources and documentation. Documentation updates should avoid marketing language, state the technical problem in plain terms, and include a materials list when relevant.

- If `make format` fails in the sandbox because `uv` cannot write under `~/.local/share/uv/tools`, run the equivalent `.venv/bin/isort`, `.venv/bin/ruff check --fix`, and `cargo fmt` commands for the touched files and record both the failure and the fallback commands in Recent Validation.

- Avoid declaring module-level `__all__` exports. Prefer explicit imports at call sites instead of curating export lists.

- Define device identifiers (such as firmware `device_name` values) as module-level constants instead of inline literals.

- Avoid building filesystem paths via string concatenation. Use `os.path.join` or `pathlib.Path` instead.

- Prefer `pathlib.Path` over `os.path.join` when constructing paths, and ensure functions annotated to return a `Path` return a `Path` object.

- Keep standalone distributable packages under `packages/<distribution-name>/src/<import_name>` and wire them into the root project with local `tool.uv.sources` path dependencies.

- When CLI arguments accept file paths, parse them as `pathlib.Path` objects and ensure parent directories exist before writing.

- Avoid using `print` for runtime diagnostics in CLI commands; use the shared logger.

- Avoid using `print` for runtime diagnostics in peripheral modules; use the shared logger.

- Avoid leaving commented debug print statements in production modules; remove them once they are no longer needed.

- Avoid wildcard imports; use explicit imports so dependencies stay clear and tooling can track usage.

- Prefer f-strings over string concatenation when assembling Python strings so formatting stays readable.

- Prefer mixins or composition over direct inheritance.

- Prefer direct code over single-use wrapper layers; when a helper only forwards to one caller, inline it or delete it.

- Prefer `StrEnum` values over raw strings for strategy/configuration mode selections.

- Use `heart.utilities.logging.get_logger` for internal logging so handlers and log levels stay consistent across modules.

- Prefer `get_logger(__name__)` so loggers include their module path and stay consistent in telemetry.
- For Pi 5 scan transport work, benchmark both `resident_refresh_hz` and `distinct_frame_update_hz` on a real Pi before keeping an architectural rewrite; smaller packed words alone are not sufficient if the change adds FIFO streams or MMIO fan-out.

- When starting background threads, provide a descriptive name via `threading.Thread(name=...)` to keep diagnostics readable.

- Prefer module-level constants for tunable loop intervals instead of embedding sleep literals in long-running loops.

- Prefer module-level constants for retry thresholds in reconnect or recovery loops instead of inline numeric literals.

- Use `time.monotonic()` for elapsed-time comparisons, and reserve `time.time()` for wall-clock timestamps that leave the process.

- Prefer `pathlib.Path.read_text`/`write_text` helpers for straightforward text file I/O.

- When reading or writing text files, specify an explicit encoding (prefer `utf-8`) to keep behavior consistent across platforms.

- Use module-level constants for public constructor or function default values so shared configuration is easy to discover and adjust.

- When parsing PATH-like environment variables, strip whitespace from each entry and ignore empty values before converting to `Path` objects.

- Use module-level constants for default argument values and shared string literals in firmware helpers so configuration stays consistent.

- Keep mixed PyO3 packages in a `rust/<package>/` layout with the Rust crate in `src/`, Python shims in `python/<package>/`, and a private native submodule exposed via `tool.maturin.module-name` to avoid import ambiguity.
- Keep mixed PyO3 package names aligned across the crate name, the Python import package, `tool.maturin.module-name`, and the root `tool.uv.sources` entry so renames do not leave split namespaces behind.

- Keep PyO3 `extension-module` support behind a crate feature that Maturin enables for extension builds so `cargo test` and `cargo bench` can link without Python-extension linker behavior.

- After changing exported PyO3 classes or Python shims under `rust/<package>/python/`, rerun `uv sync --extra native` before Python validation so the active virtualenv rebuilds the local extension with the matching symbols.

- PyO3 stub-generation binaries require a linkable Python 3.11 runtime in addition to Rust; if `cargo run --bin stub_gen` fails with unresolved `Py*` symbols, install or point PyO3 at a Python 3.11 build before retrying.
- On non-Linux aarch64 hosts, the Pi 5 RP1/PIOLib shims are `cfg`-gated out of Rust builds. Validate Pi 5 scan transport changes here with the Rust-side trace/simulation tests, and explicitly call out when the native shim has not been compiled on real Pi hardware.
- Keep PyO3 stub-generation dependencies behind an explicit Cargo feature so Pi-side benchmarks and other non-stub binaries do not compile `pyo3-stub-gen` and its parser stack unnecessarily.
- Rust runtime tuning knobs should live in `rust/heart_rgb_matrix_driver/src/runtime/tuning.rs` and use `HEART_*` environment variables instead of file-local magic constants.
- For out-of-tree Pi kernel modules, prefer exported scheduler helpers such as `sched_setattr_nocheck()`; `sched_setscheduler_nocheck()` is not exported on the Raspberry Pi kernel and will fail at `modpost`.

- Guard Raspberry Pi `pinctrl` integration tests behind `HEART_RUN_PI5_PINCTRL_TESTS=1` and mark them with `pytest.mark.integration` so normal desktop test runs stay hardware-free.

- Pi 5 DMA/PIO benchmark work depends on `libpio-dev` being installed on the target host so the native transport shim can compile and link against `libpio`.

- For the Pi 5 HUB75 path on the Adafruit bonnet pin map, scan-format compaction mainly reduces per-group control words; each shifted column still needs a full sparse pin-state word unless the output strategy is redesigned around different pin grouping.
- For the Pi 5 HUB75 path, keep the simple bring-up transport on the synthetic `GPIO 4..27` bus so it can drive `AdafruitHat` (`OE=GPIO4`) while keeping `GPIO4` low by default for `AdafruitHatPwm`; keep the optimized resident parser on its existing packed `GPIO 5..27` bus unless the packed transport format is redesigned too.
- For the Pi 5 `AdafruitHatPwm` bonnet, leave bridged `GPIO4` unclaimed in the simple native `/dev/pio0` output window when `OE` is on `GPIO18`; driving both ends of the bonnet bridge as outputs causes live color/refresh corruption.
- For the Pi 5 `AdafruitHatPwm` simple command stream, mirror OE onto both `GPIO18` and bridged `GPIO4` in the emitted literal GPIO words, but keep `GPIO4` unclaimed in the native shim so the bridge follows `GPIO18` without two driven outputs fighting.
- For Pi 5 HUB75 bring-up, keep the simple path host-driven: stream literal GPIO words plus compact data/delay commands, keep `LAT` and `OE` in-band in those words, and let PIO own only the repeated `CLK` pulse via side-set.
- For Pi 5 HUB75 bring-up, prove new PIO waveforms and config changes in Adafruit Piomatter (or a minimal Piomatter-equivalent probe) on the same panel and wiring before porting them into `heart_rgb_matrix_driver`; treat the in-repo port as unproven until it matches the Piomatter baseline.
- Use `scripts/piomatter_rgb_cycle.py` as the default Piomatter parity probe for Pi 5 full-screen color-turnover checks before debugging the in-repo driver path.
- For Pi 5 HUB75 commands, demos, and runtime defaults, prefer `adafruit-hat-pwm` unless the user explicitly asks for a different hardware mapping or Piomatter pinout.
- On Pi 5 raw `/dev/pio0` submits, `PIO_IOC_SM_XFER_DATA` can return `ETIMEDOUT` even when the state machine continues consuming the submitted buffer; match Piomatter by treating that submit-time timeout as soft success and rely on the explicit drain/wait step to detect real TX stalls.
- For Pi 5 checkpoint demos, treat the raw `/dev/pio0` shim and the kernel resident loop as separate transports: the raw shim currently speaks only the simple host-driven command stream, while `Pi5ScanFormat::Optimized` demos must go through `Pi5KernelResidentLoop` until the raw shim grows an optimized parser.

- The compact `Pi5ScanFormat::Simple` row engine uses `jmp x--` / `jmp y--` loops in PIO, so its transport must encode shift-count and dwell-count control words as `logical_count - 1`; validate changes to that contract with the Rust opcode-level PIO simulator before retesting on hardware.
- Keep the generic opcode-level PIO simulator in the standalone Rust lib `rust/heart_pio_sim`; `heart_rgb_matrix_driver` should only keep Pi 5-specific wrapper code that builds programs/config for that shared simulator.
- Keep generic opcode-level simulator property tests under `rust/heart_pio_sim/tests/`; keep Pi 5 panel-specific waveform and timing assertions in `rust/heart_rgb_matrix_driver/src/runtime/tests.rs`.
- Keep Pi 5 PIO programs in `rust/heart_rgb_matrix_driver/pio/*.pio` as the readable source of truth; regenerate the checked-in C and Rust opcode outputs with `rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py` instead of hand-editing instruction tables in the shim, kernel module, or simulator wrappers.
- Keep Pi 5 audit and trace tools wired to shared generated-program helpers from `rust/heart_rgb_matrix_driver/src/runtime/pi5_pio_sim.rs` so they do not carry stale handwritten opcode tables alongside the real `.pio` source.

- The Pi 5 resident scan-buffer loop is the correct way to skip unchanged frames: keep replaying the active packed frame until a new one arrives instead of leaving the panel idle between updates.

- Keep Rust unit tests hardware-agnostic under `cfg(test)` even on Raspberry Pi hosts; validate real Pi 5 scanout and transport behavior through the Pi-only Python tests and benchmark targets instead of unit-test backend auto-detection.

## Dependency Injection (Lagom)

- Prefer using `heart.runtime.container.build_runtime_container` and the provider registration helpers in
  `heart.peripheral.core.providers` rather than instantiating new Lagom `Container` objects in feature modules.
- Only `heart.runtime.container` and `heart.peripheral.core.providers` should import Lagom types directly. Other
  modules should depend on `heart.runtime.container.RuntimeContainer` when they need to reference the container
  type so dependency wiring stays centralized.
- Feature modules should register singleton bindings with provider helpers (for example,
  `register_singleton_provider`) instead of reaching for Lagom bindings directly, so container usage remains
  encapsulated.
- When tracking runtime containers for provider registration updates, use weak references (for example,
  `weakref.WeakSet`) so stale containers do not stay alive after shutdown.
- Define Lagom provider factories as named module-level callables instead of inline lambdas so the dependency
  graph stays discoverable and traceable in logs and stack traces.

## Error Handling

- Avoid catching `BaseException`; catch `Exception` or more specific error types so system-exiting signals propagate.
- Avoid raising bare `Exception` values; raise more specific exception types instead.
- Avoid swallowing recoverable exceptions; log them with the shared logger (use debug-level logging for high-frequency loops).
- When logging caught exceptions at error level, include traceback context with `logger.exception` (or `exc_info=True`) unless you intentionally suppress it.
- Avoid placeholder early returns or unreachable docstrings in production methods; remove stubs once real implementations are available.
- For CLI commands, log expected user-facing errors and exit with `typer.Exit` instead of raising generic exceptions.

## Testing

Run `make test` to execute the Pytest suite located in the `tests/` directory. Ensure all tests pass before submitting changes.

When adding tests, it is reasonable to stub hardware- or framework-heavy dependencies so the core logic can be exercised in isolation.

- Parameterize new Pytest cases when validating multiple input permutations (especially for driver behaviour) to keep coverage
  clear and maintainable.
- Write tests with precise assertions and include a short docstring at the top of every test function that explains the behaviour
  being validated. Each test docstring must mention both the specific behaviour under test and why that behaviour matters at a
  higher level (e.g., performance, resilience, integration, documentation value). Group related tests into descriptive `pytest`
  classes and give every class a docstring that states the shared focus and broader value of the grouped scenarios so readers can
  immediately understand the collection's intent.
- When protocol or transport formats gain variable-length commands, update the simulator and tests to parse the stream semantically instead of depending on fixed word indices or a single-command layout.

## Linting (Optional Pre-Check)

Running `make check` is recommended to verify formatting and linting without applying fixes.

## Documentation Guidelines

Update `docs/code_flow.md` and re-render the diagram with `scripts/render_code_flow.py` whenever runtime architecture changes. Pair thoughtful implementations with a research note under `docs/research/` that cites the relevant source files. Plans under `docs/planning/` must follow the structure defined in their `AGENTS.md` file, and contributors should review the master project list in `docs/planning/README.md` before scoping new work. Before implementing a proposal, check `docs/planning/` for related higher-level plans and review any relevant documents.

## File Focus

Keep files narrowly scoped. If a module starts handling multiple concerns (e.g., orchestration plus parsing plus rendering), split the responsibilities into smaller, purpose-built modules so each file does slightly fewer things and has a clear single focus.
- When a Pi 5 scanout experiment stops being the supported path, delete the retired module params, probe binaries, and alternate replay branches instead of leaving dormant tuning code behind.
- When a device helper module is no longer imported anywhere in-tree, delete the orphaned file and any lazy exports that only kept it alive instead of preserving dead compatibility shims indefinitely.

## Environment Cleanup

If a single file or module starts accumulating setup, orchestration, configuration, and cleanup logic, move those responsibilities into smaller, focused modules or helpers so each file handles fewer concerns and is easier to reason about.

## Configuration Defaults

Define CLI default values as module-level constants so they stay consistent across commands and are easy to review.

- For the Pi 5 simple HUB75 bring-up path, keep the simulator aligned with the native shim's last-mile behavior: model the submit-time literal-word lowering into real GPIO positions and consume the SM pin/sideset config from the shared generated `.pio` metadata rather than duplicating those values by hand.

## Validation

- `make format`
- `make test`

## Recent Validation

- `2026-04-01`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hub75_probe`
- `2026-04-01`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_group_words_keep_gpio4_in_sync_with_oe_for_adafruit_hat_pwm -- --nocapture`
- `2026-04-01`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_native_pwm_bonnet_keeps_gpio4_out_of_the_pio_output_window -- --nocapture`
- `2026-04-01`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make format`
- `2026-04-01`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-04-01`: Remote on `michael@totem1.local`: `~/.cargo/bin/cargo build --release --bin pi5_pio_hub75_probe`
- `2026-04-01`: Remote on `michael@totem1.local`: `strace -f -tt -e trace=ioctl,openat,close ./target/release/pi5_pio_hub75_probe ...` showed `PIO_IOC_SM_XFER_DATA` returning `ETIMEDOUT`; matching `strace` on `scripts/piomatter_rgb_cycle.py` showed the same ioctl timeout while Piomatter still rendered.
- `2026-04-01`: Remote on `michael@totem1.local`: `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat-pwm --pattern blue-fill --hold-seconds 10 --frame-seconds 10 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`
- `2026-04-01`: Remote on `michael@totem1.local`: `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat-pwm --pattern blue-fill --hold-seconds 3 --frame-seconds 3 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`
- `2026-04-01`: Remote on `michael@totem1.local`: after leaving bridged `GPIO4` unclaimed for `AdafruitHatPwm`, the same `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat-pwm --pattern blue-fill --hold-seconds 3 --frame-seconds 3 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64` command increased `completed_row_pair_scans` from `96` to `46464`.
- `2026-04-01`: Remote on `michael@totem1.local`: after mirroring `OE` onto `GPIO4` and `GPIO18` in the simple `AdafruitHatPwm` command words, reran `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat-pwm --pattern blue-fill --hold-seconds 3 --frame-seconds 3 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`, which completed with `completed_row_pair_scans=70080`.
- `2026-04-01`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_checkpoint_demo`
- `2026-04-01`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make format`
- `2026-04-01`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-04-01`: `uv pip install --python .venv/bin/python adafruit-circuitpython-pioasm`
- `2026-04-01`: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`
- `2026-04-01`: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py --check`
- `2026-04-01`: `uv lock`
- `2026-04-01`: `cargo fmt --manifest-path rust/heart_pio_sim/Cargo.toml`
- `2026-04-01`: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-04-01`: `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`
- `2026-04-01`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`
- `2026-04-01`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-04-01`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_sim_trace`
- `2026-04-01`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_pio_audit --bin pi5_pio_sim_trace`
- `2026-04-01`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_pio_program_summaries_track_generated_program_shapes -- --nocapture`
- `2026-04-01`: `.venv/bin/pytest -n0 tests/device/test_heart_rgb_matrix_driver_matrix_api.py tests/device/test_heart_rgb_matrix_driver_rgbmatrix_compat.py tests/device/test_rgb_display_transfer_benchmark.py`
- `2026-04-01`: `make format`
- `2026-04-01`: `make test`
- `2026-04-01`: Remote on `michael@totem1.local`: `~/.cargo/bin/cargo check --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_sim_trace`
- `2026-04-01`: Remote on `michael@totem1.local`: `make -j4` in `/home/michael/heart/rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hello`
- `2026-03-31`: Remote on `michael@totem1.local`: `cargo build --release --bin pi5_pio_hello` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-31`: Remote on `michael@totem1.local`: `pi5_pio_hello --gpio 17 --mode autoblink --clock-divider 1000000 --hold-seconds 10` and `pi5_pio_hello --gpio 17 --mode fifo --hold-seconds 9 --toggle-period-ms 3000`; both visibly toggled in `pinctrl get 17`, confirming the minimal RP1 PIO path works once pin setup relies on `pio_gpio_init()` instead of forcing `GPIO_FUNC_PIO0`.
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_smoke`
- `2026-03-31`: Remote on `michael@totem1.local`: `cargo build --release --bin pi5_pio_smoke` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-31`: Remote on `michael@totem1.local`: repeated `pi5_pio_smoke` static-word probes and one-row smoke probes while debugging the simple raw Pi 5 shim; switched the simple FIFO feed from non-blocking `pio_sm_put()` to blocking `pio_sm_put_blocking()` after confirming PIOLib drops words on FIFO overflow.
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_trace_decoder_matches_expected_simple_group_trace`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_trace_diff`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_pio_audit`
- `2026-03-31`: on `michael@totem1.local`, `~/.local/bin/uv pip install --python .venv/bin/python --reinstall -e ./rust/heart_rgb_matrix_driver`
- `2026-03-31`: on `michael@totem1.local`, `make -j4` in `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop`
- `2026-03-31`: on `michael@totem1.local`, the new Pi 5 waveform rewrite rebuilt cleanly but the raw `/dev/pio0` backend still failed at open time with `RuntimeError: Failed to load the Pi 5 scan PIO program.`; the resident `/dev/heart_pi5_scan_loop` path remains blocked by a separate RP1 instruction-space/setup issue.
- `2026-03-31`: on `michael@totem1.local`, after adding a one-time `pio_clear_instruction_memory()` retry in the raw shim, `scripts/hub75_color_cycle.py` ran on backend `pi5-adafruit-hat-pwm` and logged repeated Pi 5 submits, but the physical panel still remained dark. This means the current blocker is back to PIO waveform correctness rather than raw program-load failure.
- `2026-03-31`: `pi5_scan_trace_diff` matched locally and on `michael@totem1.local` for `--format simple --frame-pattern solid --row-pair 0 --plane-index 0`, confirming the packed Rust group words decode back to the expected GPIO-smoke semantics. `pi5_scan_pio_audit` showed the remaining structural risk: the shared PIO program still drives a contiguous `OUT PINS` window `GPIO 5..27`, which overlaps both `LAT` on GPIO 21 and side-set `CLK` on GPIO 17.

- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml` and `make test` after changing the Pi 5 runtime backend to prefer `/dev/heart_pi5_scan_loop` and use the kernel resident loop when available.
- `2026-03-31`: Remote on `michael@totem1.local`: `~/.local/bin/uv sync --extra native`, `./.venv/bin/python - <<'PY' ... driver.stats().backend_name ... PY`, and `./.venv/bin/python scripts/hub75_color_cycle.py --rows 64 --cols 64 --chain-length 1 --parallel 1 --iterations 1 --step-hold-seconds 2.0 --resubmit-interval-seconds 0.2 --intensities 255` after rebuilding `heart_rgb_matrix_driver` to confirm the active Pi backend is `pi5-adafruit-hat-pwm-kernel-loop`.
- `2026-03-31`: `./.venv/bin/python scripts/hub75_color_cycle.py --help >/dev/null` and remote `./.venv/bin/python scripts/hub75_color_cycle.py --rows 64 --cols 64 --chain-length 1 --parallel 1 --iterations 1 --step-hold-seconds 3.0 --intensities 255` after teaching the color-cycle bring-up script to submit once per step on persistent kernel-loop backends instead of repeatedly restarting the resident scan loop.
- `2026-03-31`: Remote on `michael@totem1.local`: `make -j4` in `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop`, `sudo rmmod heart_pi5_scan_loop || true`, and `sudo insmod .../heart_pi5_scan_loop.ko clock_divider_integer=4 post_addr_ticks=12 latch_ticks=2 post_latch_ticks=6 ...` after adding conservative timing module parameters to the Pi 5 kernel resident loop for panel bring-up debugging.
- `2026-03-31`: Remote on `michael@totem1.local`: `./.venv/bin/python scripts/hub75_color_cycle.py --rows 64 --cols 64 --chain-length 1 --parallel 1 --iterations 1 --step-hold-seconds 3.0 --intensities 255` after reloading the kernel resident loop with conservative timing parameters.
- `2026-03-31`: `./.venv/bin/python scripts/hub75_color_cycle.py --help`, remote `scp scripts/hub75_color_cycle.py michael@totem1.local:/home/michael/heart/scripts/hub75_color_cycle.py`, and remote `./.venv/bin/python scripts/hub75_color_cycle.py --rows 64 --cols 64 --chain-length 1 --parallel 1 --iterations 1 --step-hold-seconds 2.0 --resubmit-interval-seconds 0.2 --intensities 64,255` after changing the color-cycle bring-up script to continuously resubmit each step on the Pi 5 one-shot path.
- `2026-03-31`: Remote on `michael@totem1.local`: `HEART_PI5_SCAN_CLOCK_DIVIDER=4 HEART_PI5_SCAN_POST_ADDR_TICKS=12 HEART_PI5_SCAN_LATCH_TICKS=2 HEART_PI5_SCAN_POST_LATCH_TICKS=6 HEART_PI5_SCAN_LSB_DWELL_TICKS=4 ./.venv/bin/python scripts/hub75_color_cycle.py --rows 64 --cols 64 --chain-length 1 --parallel 1 --iterations 1 --step-hold-seconds 2.0 --resubmit-interval-seconds 0.2 --intensities 255` to check whether a much slower Pi 5 waveform changes panel bring-up behavior.
- `2026-03-31`: `make format`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, and `make test` after making Pi 5 scan format codec helpers explicitly `pub(crate)`.
- `2026-03-31`: `make format`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, and `make test` after adding maintainer-level comments across `rust/heart_rgb_matrix_driver/src/runtime/pi5_scan.rs`.
- `2026-03-31`: `make format`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, and `make test` after splitting `write_scan_segment()` into small private helpers without changing the packed Pi 5 scan format.
- `2026-03-31`: `make format`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, and `make test` after deleting dead Pi 5 scan helper constructors/wrappers.
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `make format`
- `2026-03-31`: `make test`
- `2026-03-31`: `pytest -n0 tests/device/test_heart_rgb_matrix_driver_rgbmatrix_compat.py`
- `2026-03-31`: `make format`
- `2026-03-31`: `make test`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`
- `2026-03-31`: `make format`
- `2026-03-31`: `make test`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `make format`
- `2026-03-31`: `make test`
- `2026-03-30`: `cargo check` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `make format`
- `2026-03-30`: `make test`
- `2026-03-30`: `cargo run --bin stub_gen` in `rust/heart_rgb_matrix_driver` failed at link time because this environment still lacks a linkable Python 3.11 runtime for PyO3 stub generation.
- `2026-03-30`: `uv lock`
- `2026-03-30`: `.venv/bin/isort src tests`, `.venv/bin/ruff check --fix src tests`, `.venv/bin/docformatter -i -r --config ./pyproject.toml docs`, and `.venv/bin/mdformat docs` after `make format` hit a sandboxed uv cache permission error.
- `2026-03-30`: `.venv/bin/pytest` after `make test` hit the same sandboxed uv cache permission error.
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/yolisten/renderer.py tests/renderers/test_yolisten_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/yolisten/renderer.py tests/renderers/test_yolisten_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_yolisten_renderer.py`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/text/renderer.py tests/renderers/test_text_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/text/renderer.py tests/renderers/test_text_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_text_renderer.py`
- `2026-03-30`: `.venv/bin/isort src/heart/runtime/rendering/renderer_processor.py src/heart/runtime/rendering/timing.py tests/runtime/test_renderer_processor.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/runtime/rendering/renderer_processor.py src/heart/runtime/rendering/timing.py tests/runtime/test_renderer_processor.py`
- `2026-03-30`: `.venv/bin/pytest tests/runtime/test_renderer_processor.py`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/spritesheet_random/renderer.py src/heart/programs/configurations/lib_2025.py tests/renderers/test_spritesheet_random_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/spritesheet_random/renderer.py src/heart/programs/configurations/lib_2025.py tests/renderers/test_spritesheet_random_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_spritesheet_random_renderer.py`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/isort src/heart/navigation/game_modes.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/navigation/game_modes.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/pytest tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py` after preserving `FractalScene`'s OPENGL mode and nested runtime reinitialization.
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-31`: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_`
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_scan_trace_decoder_matches_expected_simple_group_trace`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hub75_probe`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_trace_diff`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_pio_audit`
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `make format`
- `2026-03-31`: `make test`
- `2026-03-31`: `make format` and `make test` after rewriting `rust/heart_rgb_matrix_driver/README.md`.
- `2026-03-31`: `make format` and `make test` after simplifying the Pi 5 `/dev/pio0` availability check in `rust/heart_rgb_matrix_driver/src/runtime/backend.rs`.
- `2026-03-31`: `make format` and `make test` after centralizing Rust runtime tuning knobs in `rust/heart_rgb_matrix_driver/src/runtime/tuning.rs` and making the PyO3 boundary parse `WiringProfile` / `ColorOrder` before calling `MatrixDriverCore::new`.
- `2026-03-30`: Remote on `michael@totem1.local`: `make install-pi5-scan-loop-module`, `PIO_BENCH_CHAIN_LENGTH=4 PIO_BENCH_ITERATIONS=3 PIO_BENCH_FRAME_COUNT=16 make bench-matrix-pio-scan`, and `HEART_RUN_PI5_SCAN_TESTS=1 .venv/bin/pytest -n0 tests/device/test_rgb_display_pi5_scan_benchmark.py`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --pwm-bits 11 --iterations 3 --frame-count 16 --resident-loop-ms 100 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: `.venv/bin/isort src/heart/utilities/env/rendering.py src/heart/runtime/rendering/renderer_processor.py tests/runtime/test_renderer_processor.py tests/utilities/test_env.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/utilities/env/rendering.py src/heart/runtime/rendering/renderer_processor.py tests/runtime/test_renderer_processor.py tests/utilities/test_env.py`
- `2026-03-30`: `.venv/bin/pytest tests/runtime/test_renderer_processor.py`
- `2026-03-30`: `.venv/bin/pytest tests/utilities/test_env.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py` after removing fractal runtime display-context reconfiguration.
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/navigation/composed_renderer.py src/heart/navigation/game_modes.py tests/navigation/test_composed_renderer_resolution.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/navigation/composed_renderer.py src/heart/navigation/game_modes.py tests/navigation/test_composed_renderer_resolution.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/pytest tests/navigation/test_composed_renderer_resolution.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/three_fractal/provider.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/three_fractal/provider.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_three_fractal_renderer.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/hilbert_curve/provider.py src/heart/renderers/hilbert_curve/renderer.py tests/renderers/test_hilbert_curve_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/hilbert_curve/provider.py src/heart/renderers/hilbert_curve/renderer.py tests/renderers/test_hilbert_curve_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_hilbert_curve_renderer.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/runtime/display_context.py src/heart/runtime/game_loop/__init__.py src/heart/navigation/game_modes.py tests/runtime/test_display_context.py tests/runtime/test_game_loop.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/runtime/display_context.py src/heart/runtime/game_loop/__init__.py src/heart/navigation/game_modes.py tests/runtime/test_display_context.py tests/runtime/test_game_loop.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `.venv/bin/pytest tests/runtime/test_display_context.py tests/runtime/test_game_loop.py tests/navigation/test_game_modes.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make format` failed because `uv` could not write under `/Users/lampe/.local/share/uv/tools/.tmp1CLZAq` in this sandbox.
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-30`: `.venv/bin/isort src/heart/renderers/hilbert_curve/provider.py tests/renderers/test_hilbert_curve_renderer.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/renderers/hilbert_curve/provider.py tests/renderers/test_hilbert_curve_renderer.py`
- `2026-03-30`: `.venv/bin/pytest tests/renderers/test_hilbert_curve_renderer.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache make test`
- `2026-03-31`: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`
- `2026-03-31`: `.venv/bin/isort rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/matrix.py rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/__init__.py src/heart/device/rgb_display/runtime.py tests/device/test_heart_rgb_matrix_driver_matrix_api.py`
- `2026-03-31`: `.venv/bin/ruff check --fix rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/matrix.py rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/__init__.py src/heart/device/rgb_display/runtime.py tests/device/test_heart_rgb_matrix_driver_matrix_api.py`
- `2026-03-31`: `.venv/bin/pytest -n0 tests/device/test_heart_rgb_matrix_driver_matrix_api.py tests/device/test_rgb_display_runtime.py`
- `2026-03-31`: `UV_CACHE_DIR=/Users/lampe/.codex/worktrees/b4c5/heart/.uv-cache .venv/bin/mdformat docs/research/pi5_scan_transport_layers.md`
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`
- `2026-03-31`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`
- `2026-03-31`: Remote Pi kernel rebuild with `ssh michael@totem1.local 'cd /home/michael/heart/rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop && make -j4'` after the C-file documentation pass.
- `2026-03-31`: Remote Pi Rust check with `ssh michael@totem1.local 'cd /home/michael/heart/rust/heart_rgb_matrix_driver && ~/.cargo/bin/cargo check --bin pi5_scan_bench'` after the C-file documentation pass.
- `2026-03-31`: `bash -n packages/heart-device-manager/src/heart_device_manager/install_rgb_matrix.sh packages/heart-device-manager/src/heart_device_manager/install_pi5_scan_loop_dkms.sh`
- `2026-03-31`: `uv build ./rust/heart_rgb_matrix_driver --wheel --out-dir /tmp/heart-rgb-matrix-driver-dist -v` confirmed the maturin backend used `profile` from `rust/heart_rgb_matrix_driver/pyproject.toml` and finished a `release` build.
- `2026-03-31`: Remote Pi kernel rebuild with `ssh michael@totem1.local 'cd /home/michael/heart/rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop && make -j4'`
- `2026-03-31`: Remote Pi dense benchmark with `ssh michael@totem1.local 'cd /home/michael/heart && ~/.cargo/bin/cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 11 --iterations 1 --frame-count 8 --resident-loop-ms 200 --frame-pattern dense'` after reloading `heart_pi5_scan_loop.ko batch_target_bytes=2097152`
- `2026-03-30`: `cargo fmt` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `.venv/bin/isort src/heart/device/rgb_display/device.py src/heart/device/rgb_display/runtime.py tests/device/test_rgb_display_runtime.py rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/__init__.py rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/matrix.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/device/rgb_display/device.py src/heart/device/rgb_display/runtime.py tests/device/test_rgb_display_runtime.py rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/__init__.py rust/heart_rgb_matrix_driver/python/heart_rgb_matrix_driver/matrix.py`
- `2026-03-30`: `.venv/bin/mdformat AGENTS.md`
- `2026-03-30`: `cargo check` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `python -m pytest tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/.codex/worktrees/b4c5/heart/.uv-cache make format` failed because `uv` still attempted to write under `/Users/lampe/.local/share/uv/tools/.tmpwbJ6BV` in this sandbox.
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/.codex/worktrees/b4c5/heart/.uv-cache make test`
- `2026-03-30`: `cargo fmt` in `rust/heart_rgb_matrix_driver` after splitting `lib.rs` into a thin PyO3 API and `src/runtime.rs`
- `2026-03-30`: `cargo check` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `python -m pytest tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `cargo fmt` in `rust/heart_rgb_matrix_driver` after adding detached PyO3 matrix calls, Rayon-backed color remapping, unit tests, and the `matrix_transfer` benchmark.
- `2026-03-30`: `cargo fmt` in `rust/heart_rgb_matrix_driver` after adding the Pi 5 scan scheduler, FIFO-fed scan shim, and `pi5_scan_bench`.
- `2026-03-30`: `cargo check --tests --benches` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `cargo test` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `python scripts/render_code_flow.py --output docs/code_flow.svg`
- `2026-03-30`: remote Pi 5 `cargo test` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: remote Pi 5 `cargo bench --bench matrix_transfer -- --quick` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: remote Pi 5 `make bench-matrix-pio-dma PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=1 PIO_BENCH_PARALLEL=1 PIO_BENCH_PWM_BITS=11 PIO_BENCH_ITERATIONS=5 PIO_BENCH_FRAME_COUNT=64 PIO_BENCH_PIPELINE_DEPTH=2`
- `2026-03-30`: remote Pi 5 `make bench-matrix-pio-dma PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=4 PIO_BENCH_PARALLEL=1 PIO_BENCH_PWM_BITS=11 PIO_BENCH_ITERATIONS=5 PIO_BENCH_FRAME_COUNT=64 PIO_BENCH_PIPELINE_DEPTH=2`
- `2026-03-30`: remote Pi 5 `make bench-matrix-pio-scan PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=1 PIO_BENCH_PARALLEL=1 PIO_BENCH_ITERATIONS=1 PIO_BENCH_FRAME_COUNT=1 PIO_BENCH_PIPELINE_DEPTH=1`
- `2026-03-30`: remote Pi 5 `make bench-matrix-pio-scan PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=4 PIO_BENCH_PARALLEL=1 PIO_BENCH_ITERATIONS=1 PIO_BENCH_FRAME_COUNT=1 PIO_BENCH_PIPELINE_DEPTH=1`
- `2026-03-30`: remote Pi 5 `make test-matrix-pio-scan`
- `2026-03-30`: remote Pi 5 `.venv/bin/python -m pytest -n0 tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo test` in `rust/heart_rgb_matrix_driver` failed at link time because this environment still lacks a linkable Python 3.11 runtime for PyO3 test binaries.
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo bench --bench matrix_transfer -- --quick` in `rust/heart_rgb_matrix_driver` failed at link time for the same missing Python 3.11 runtime.
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo check --tests --benches` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `python -m pytest tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo test` in `rust/heart_rgb_matrix_driver` after gating PyO3 `extension-module` behind the `python-extension` feature and enabling it only through Maturin.
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo bench --bench matrix_transfer -- --quick` in `rust/heart_rgb_matrix_driver` after the same PyO3 packaging fix.
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo run --bin stub_gen` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `python -m pytest tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `cargo fmt` in `rust/heart_rgb_matrix_driver` after splitting the clean-room matrix runtime into `runtime/` submodules, adding frame-buffer pooling, and expanding the benchmark surface.
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo check --tests --benches` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `.venv/bin/isort src/heart/device/rgb_display/runtime.py tests/device/test_rgb_display_runtime.py tests/device/test_rgb_display_transfer_benchmark.py scripts/render_code_flow.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/device/rgb_display/runtime.py tests/device/test_rgb_display_runtime.py tests/device/test_rgb_display_transfer_benchmark.py scripts/render_code_flow.py`
- `2026-03-30`: `.venv/bin/mdformat docs/code_flow.md docs/research/hub75_clean_room_runtime_prd.md`
- `2026-03-30`: `python scripts/render_code_flow.py --output docs/code_flow.svg`
- `2026-03-30`: `python -m py_compile /Users/lampe/code/settings/codex/skills/rpi5-hub75-bringup/scripts/remote_hub75_check.py`
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo test` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo bench --bench matrix_transfer -- --quick` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `CARGO_HOME=/Users/lampe/.codex/worktrees/b4c5/heart/.cargo-home cargo run --bin stub_gen` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `python -m pytest -n0 tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `UV_CACHE_DIR=/Users/lampe/.codex/worktrees/b4c5/heart/.uv-cache uv sync --extra native`
- `2026-03-30`: `python -m pytest -n0 tests/device/test_rgb_display_transfer_benchmark.py --benchmark-min-rounds=3 --benchmark-max-time=0.02`
- `2026-03-30`: `.venv/bin/isort src/heart/device/rgb_display/debug.py tests/device/test_rgb_display_pinctrl_debug.py`
- `2026-03-30`: `.venv/bin/ruff check --fix src/heart/device/rgb_display/debug.py tests/device/test_rgb_display_pinctrl_debug.py`
- `2026-03-30`: `python -m pytest -n0 tests/device/test_rgb_display_pinctrl_debug.py` (skipped on the local macOS host because `HEART_RUN_PI5_PINCTRL_TESTS` was unset and `pinctrl` is Pi-specific)
- `2026-03-30`: `python -m py_compile /Users/lampe/code/settings/codex/skills/rpi5-hub75-bringup/scripts/remote_hub75_check.py`
- `2026-03-30`: Remote on `michael@totem1.local`: `make bootstrap-native`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo test` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo bench --bench matrix_transfer -- --quick` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --bin stub_gen` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: Remote on `michael@totem1.local`: `make debug-matrix-pinctrl`
- `2026-03-30`: Remote on `michael@totem1.local`: `make test-matrix-pinctrl`
- `2026-03-30`: Remote on `michael@totem1.local`: `HEART_RUN_PI5_PINCTRL_TESTS=1 .venv/bin/python -m pytest -n0 tests/device/test_rgb_display_runtime.py tests/device/test_rgb_display_pinctrl_debug.py tests/device/test_rgb_display_transfer_benchmark.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `cargo fmt` in `rust/heart_rgb_matrix_driver` after adding the Pi 5 DMA/PIO transport probe, `libpio` C shim, transport benchmark binary, and Pi-only integration coverage.
- `2026-03-30`: `cargo check --tests --benches` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `cargo test` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `cargo bench --bench matrix_transfer -- --quick` in `rust/heart_rgb_matrix_driver`
- `2026-03-30`: `cargo run --bin pi5_pio_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --pwm-bits 11 --iterations 1` in `rust/heart_rgb_matrix_driver` failed as expected on the local macOS host because the Pi 5 DMA/PIO transport is only supported on Linux aarch64.
- `2026-03-30`: `python -m pytest -n0 tests/device/test_rgb_display_pi5_dma_benchmark.py tests/device/test_rgb_display_pinctrl_debug.py tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `.venv/bin/mdformat docs/code_flow.md docs/research/hub75_clean_room_runtime_prd.md AGENTS.md /Users/lampe/code/settings/codex/skills/rpi5-hub75-bringup/SKILL.md`
- `2026-03-30`: `python scripts/render_code_flow.py --output docs/code_flow.svg`
- `2026-03-30`: `python -m py_compile /Users/lampe/code/settings/codex/skills/rpi5-hub75-bringup/scripts/remote_hub75_check.py`
- `2026-03-30`: `bash -n scripts/bootstrap_native_runtime.sh`
- `2026-03-30`: Remote on `michael@totem1.local`: `make bootstrap-native`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo test` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo bench --bench matrix_transfer -- --quick` in `/home/michael/heart/rust/heart_rgb_matrix_driver`, including the new `pi5_pack_transport_rgba` and `pi5_dma_transport` groups
- `2026-03-30`: Remote on `michael@totem1.local`: `make bench-matrix-pio-dma PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=1 PIO_BENCH_PARALLEL=1 PIO_BENCH_PWM_BITS=11 PIO_BENCH_ITERATIONS=5`
- `2026-03-30`: Remote on `michael@totem1.local`: `make bench-matrix-pio-dma PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=4 PIO_BENCH_PARALLEL=1 PIO_BENCH_PWM_BITS=11 PIO_BENCH_ITERATIONS=5`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --bin stub_gen` in `/home/michael/heart/rust/heart_rgb_matrix_driver`
- `2026-03-30`: Remote on `michael@totem1.local`: `make debug-matrix-pinctrl`
- `2026-03-30`: Remote on `michael@totem1.local`: `make test-matrix-pinctrl`
- `2026-03-30`: Remote on `michael@totem1.local`: `make test-matrix-pio-dma`
- `2026-03-30`: Remote on `michael@totem1.local`: `HEART_RUN_PI5_PINCTRL_TESTS=1 HEART_RUN_PI5_PIO_TESTS=1 .venv/bin/python -m pytest -n0 tests/device/test_rgb_display_runtime.py tests/device/test_rgb_display_pinctrl_debug.py tests/device/test_rgb_display_pi5_dma_benchmark.py tests/device/test_rgb_display_transfer_benchmark.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `cargo fmt` and `cargo check` in `rust/heart_rgb_matrix_driver` after adding the pipelined full-cycle Pi 5 DMA/PIO benchmark path.
- `2026-03-30`: `python -m pytest -n0 tests/device/test_rgb_display_pi5_dma_benchmark.py` on the local macOS host (skipped because `HEART_RUN_PI5_PIO_TESTS` was unset and the benchmark is Pi-specific).
- `2026-03-30`: `python -m py_compile /Users/lampe/code/settings/codex/skills/rpi5-hub75-bringup/scripts/remote_hub75_check.py`
- `2026-03-30`: `.venv/bin/mdformat /Users/lampe/code/settings/codex/skills/rpi5-hub75-bringup/SKILL.md AGENTS.md`
- `2026-03-30`: Remote on `michael@totem1.local`: `make bench-matrix-pio-dma PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=1 PIO_BENCH_PARALLEL=1 PIO_BENCH_PWM_BITS=11 PIO_BENCH_ITERATIONS=5 PIO_BENCH_FRAME_COUNT=64 PIO_BENCH_PIPELINE_DEPTH=2`
- `2026-03-30`: Remote on `michael@totem1.local`: `make bench-matrix-pio-dma PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=4 PIO_BENCH_PARALLEL=1 PIO_BENCH_PWM_BITS=11 PIO_BENCH_ITERATIONS=5 PIO_BENCH_FRAME_COUNT=64 PIO_BENCH_PIPELINE_DEPTH=2`
- `2026-03-30`: `cargo check --tests --benches`, `cargo test`, and `cargo bench --bench matrix_transfer -- --quick` in `rust/heart_rgb_matrix_driver` after moving the Rust benchmark sources into `rust/heart_rgb_matrix_driver/bench/` and wiring Cargo paths explicitly.
- `2026-03-30`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_dma_probe --bin pi5_scan_bench`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release -p heart_rgb_matrix_driver --bin pi5_scan_dma_probe -- --max-transfer-words 1`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release -p heart_rgb_matrix_driver --bin pi5_scan_dma_probe -- --max-transfer-words 16`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release -p heart_rgb_matrix_driver --bin pi5_scan_dma_probe -- --max-transfer-words 5720`
- `2026-03-30`: Remote on `michael@totem1.local`: `make bench-matrix-pio-scan PIO_BENCH_PANEL_ROWS=64 PIO_BENCH_PANEL_COLS=64 PIO_BENCH_CHAIN_LENGTH=1 PIO_BENCH_PARALLEL=1 PIO_BENCH_ITERATIONS=1 PIO_BENCH_FRAME_COUNT=1 PIO_BENCH_PIPELINE_DEPTH=1`
- `2026-03-30`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_gpio_probe`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release -p heart_rgb_matrix_driver --bin pi5_gpio_probe -- --gpio 17 --cycles 3 --sleep-ms 50`
- `2026-03-30`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe`
- `2026-03-30`: Remote on `michael@totem1.local`: `strace -o /tmp/piolib_simple_xfer.strace -f -e trace=openat,ioctl /tmp/piolib_simple_xfer`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program simple --pattern delay --word-count 2 --max-transfer-words 16`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program scan --pattern delay --word-count 2 --max-transfer-words 16`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program simple --pattern delay --word-count 5720 --max-transfer-words 5720`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program scan --pattern delay --word-count 5720 --max-transfer-words 5720`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program scan --pattern data --word-count 5720 --max-transfer-words 5720`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program scan --pattern alternating --word-count 5720 --max-transfer-words 5720`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_ioctl_probe -- --program scan --pattern segment --word-count 5720 --max-transfer-words 5720`
- `2026-03-30`: Remote on `michael@totem1.local`: `strace -o /tmp/pi5_scan_bench.strace -f -e trace=ioctl cargo run --release --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 1 --frame-count 1 --pipeline-depth 1`
- `2026-03-30`: Remote on `michael@totem1.local`: `cargo run --release --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 1 --frame-count 1 --pipeline-depth 1` is currently blocked by repeated `rustc` SIGSEGV crashes during dependency rebuild on the Pi.
- `2026-03-30`: `cargo fmt` and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_bench --bin pi5_scan_bench` after deleting the temporary Pi probe/debug binaries and removing their probe-only runtime hooks.
- `2026-03-30`: `cargo fmt`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench --bin pi5_pio_bench` after tuning the Pi 5 scan packer and DMA submission loop.
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 5 --frame-count 32 --pipeline-depth 2`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --iterations 5 --frame-count 32 --pipeline-depth 2`
- `2026-03-30`: `cargo fmt`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba` after exposing Pi 5 scan dwell and clock-divider benchmark knobs plus derived scan-schedule metrics.
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 2 --frame-count 8 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 2 --frame-count 8 --pipeline-depth 2 --lsb-dwell-ticks 1 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --iterations 2 --frame-count 8 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 3 --frame-count 16 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.5`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 3 --frame-count 16 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.25`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 3 --frame-count 16 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 2 --frame-count 8 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 0.95`, `0.9`, `0.85`, and `0.8` to probe the lower scan-clock stability boundary; values below `1.0` either failed DMA buffer configuration (`errno=19`) or timed out during submission (`errno=110`).
- `2026-03-30`: `cargo fmt`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench --benches`, and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba` after deleting the obsolete transport-only Pi DMA benchmark path (`pi5_pio_bench`, `pi5_dma`, and related tests/targets).
- `2026-03-30`: `cargo fmt`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba` after splitting Pi 5 scan transport submission and drain into an async queue-backed transport worker while preserving the synchronous benchmark path.
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 2 --frame-count 8 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `.venv/bin/python -m pytest -n0 tests/device/test_rgb_display_runtime.py tests/navigation/test_native_scene_manager.py`
- `2026-03-30`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench` after updating the pipelined scan benchmark path to use `submit_async()` and `wait_complete()` instead of synchronous `stream()`.
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench` after exposing `pwm_bits` as a scan-benchmark knob.
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 8 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --pwm-bits 8 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: `cargo fmt`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba` after compacting the Pi 5 scan-group format so the PIO program owns latch, blanking, and dwell sequencing with five control words per row-pair / bitplane group.
- `2026-03-30`: `python scripts/render_code_flow.py --output docs/code_flow.svg`
- `2026-03-30`: `.venv/bin/pytest tests/device/test_rgb_display_pi5_scan_benchmark.py` (skipped on the local macOS host because `HEART_RUN_PI5_SCAN_TESTS` was unset)
- `2026-03-30`: Remote on `michael@totem1.local`: `/home/michael/.cargo/bin/cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --pwm-bits 11 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `/home/michael/.cargo/bin/cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 11 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `/home/michael/heart/rust/heart_rgb_matrix_driver/target/release/pi5_scan_bench --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --pwm-bits 11 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: Remote on `michael@totem1.local`: `/home/michael/heart/rust/heart_rgb_matrix_driver/target/release/pi5_scan_bench --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 11 --iterations 5 --frame-count 32 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0`
- `2026-03-30`: `cargo fmt`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, and `.venv/bin/pytest tests/device/test_rgb_display_pi5_scan_benchmark.py` after adding explicit `display_hz` and `submit_hz` fields to the Pi 5 scan benchmark output (the pytest case remained skipped locally because `HEART_RUN_PI5_SCAN_TESTS` was unset).
- `2026-03-30`: Remote on `michael@totem1.local`: `/home/michael/.cargo/bin/cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 11 --iterations 3 --frame-count 16 --pipeline-depth 2 --lsb-dwell-ticks 2 --clock-divider 1.0` to confirm the new `display_hz` versus `submit_hz` reporting on real Pi hardware.
- `2026-03-30`: `cargo fmt`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `.venv/bin/pytest tests/device/test_rgb_display_pi5_scan_benchmark.py`, `python scripts/render_code_flow.py --output docs/code_flow.svg`, and `.venv/bin/mdformat AGENTS.md docs/code_flow.md docs/research/hub75_clean_room_runtime_prd.md` after switching the Pi 5 transport worker to resident scan-buffer looping and reducing the benchmark to stable `display_hz` plus resident-loop metrics.
- `2026-03-30`: Remote on `michael@totem1.local`: `/home/michael/.cargo/bin/cargo run --release --manifest-path /home/michael/heart/rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 11 --iterations 3 --frame-count 16 --pipeline-depth 2 --resident-loop-ms 100 --lsb-dwell-ticks 2 --clock-divider 1.0` measured resident-loop first-render latency and steady-state refresh on real Pi hardware.
- `2026-03-30`: Remote on `michael@totem1.local`: rebuilt `heart_pi5_scan_loop.ko` after adding an experimental direct cyclic-DMA replay path; benchmarked `pi5_scan_bench` for `chain_length=1` and `chain_length=4` with `cyclic_dma=0` and `cyclic_dma=1`. Result: `cyclic_dma=1` slightly raised `x1` steady replay (`~489.7 Hz` vs `~479.7 Hz`) but did not improve `x4` steady replay (`~129.9 Hz`) and reduced one-shot `display_hz`, so the module default remains `cyclic_dma=0`.
- `2026-03-31`: Added `rust/heart_rgb_matrix_driver/bench/rp1_mmio_bench.c` and, on `michael@totem1.local`, compiled it with `gcc -O3 -std=c11 -Wall -Wextra`. Remote `/dev/mem` writes into RP1 shared SRAM measured about `345.3 MB/s` across `4 KiB`, `16 KiB`, `64 KiB`, and `128 KiB` regions, confirming the earlier `~48 MB/s` ceiling is specific to the PIO/DMA replay path rather than the host-to-RP1 link in general.
- `2026-03-31`: Remote on `michael@totem1.local`: rebuilt `heart_pi5_scan_loop.ko` with an experimental `sink_program` mode that drains the PIO TX FIFO without HUB75 scan logic. Benchmarks showed the same steady replay ceiling as the full scan path (`~46.6 MB/s` for `chain_length=1`, `~47.7 MB/s` for `chain_length=4`), confirming the limiter is the PIO TX replay path itself rather than the scan program.
- `2026-03-31`: Remote on `michael@totem1.local`: swept experimental `dma_maxburst` values on the direct cyclic-DMA sink path. `dma_maxburst=1`/`2` collapsed throughput to `~7.3 MB/s`, `4` reached `~29.4 MB/s`, and `8` or higher plateaued at the same `~47.7 MB/s` ceiling, so larger bursts do not break through the existing limit.
- `2026-03-31`: Remote on `michael@totem1.local`: restored the default `heart_pi5_scan_loop.ko` settings after the sink/burst experiments and re-ran `pi5_scan_bench` for `64x64`, `chain_length=4`, `parallel=1`, `pwm_bits=11`, confirming the standard resident-loop path remained at `~129.9 Hz` steady refresh.
- `2026-03-31`: Added `rust/heart_rgb_matrix_driver/bench/pi5_scan_multi_session_bench.c` and, on `michael@totem1.local`, compiled it with `gcc -O3 -std=c11 -Wall -Wextra`. In `sink_program=1` mode, aggregate replay throughput scaled past the single-channel ceiling: `1` session `~46.6 MB/s`, `2` sessions `~93.2 MB/s`, `3` sessions `~118-125 MB/s`, and `4` sessions `~141-143 MB/s`, showing the `~48 MB/s` limit is per TX channel rather than global to RP1.
- `2026-03-31`: Added `rust/heart_rgb_matrix_driver/bench/rp1_pio_fifo_mmio_bench.c` and, on `michael@totem1.local`, compiled it with `gcc -O3 -std=c11 -Wall -Wextra`. Direct MMIO writes to the RP1 PIO TX FIFO in `sink_program=1` mode measured `~159.7 MB/s`, proving the DMA replay path, not the FIFO itself, was the single-channel bottleneck.
- `2026-03-31`: Remote on `michael@totem1.local`: rebuilt `heart_pi5_scan_loop.ko` with an experimental `mmio_replay=1` path that writes packed frames directly into the PIO TX FIFO from the kernel worker. `pi5_scan_bench` then measured `~156.3 MB/s` / `~1609 Hz` steady replay for `chain_length=1` and `~157.9 MB/s` / `~429.7 Hz` steady replay for `chain_length=4`, for both `sink_program=1` and the real scan program.
- `2026-03-31`: Remote on `michael@totem1.local`: compared `mmio_replay` using scalar `writel_relaxed()` versus `iowrite32_rep()` (`mmio_rep=1`). The repeated-write helper did not improve throughput (`~157.9 MB/s` either way for `chain_length=4`) and worsened one-shot `display_hz`, so the benchmark module now defaults to `mmio_replay=1` with the scalar MMIO loop.
- `2026-03-31`: Remote on `michael@totem1.local`: compared normal device MMIO versus `ioremap_wc()` (`mmio_wc=1`) for the PIO TX FIFO in the `mmio_replay` path. Write-combining nearly doubled single-session throughput: `chain_length=1` rose from `~156.3 MB/s` to `~325.3 MB/s`, and `chain_length=4` rose from `~157.9 MB/s` / `~429.7 Hz` to `~334.2 MB/s` / `~909.4 Hz`. The kernel loop module now defaults to `mmio_wc=1`.
- `2026-03-31`: Remote on `michael@totem1.local`: multi-session sink benchmarks with `mmio_replay=1` and `mmio_wc=1` scaled aggregate replay throughput well beyond the old DMA ceiling: about `325 MB/s` for `1` session, `609-635 MB/s` for `2`, `818-845 MB/s` for `3`, and `983 MB/s` to `1.03 GB/s` for `4`, confirming the old `~48 MB/s` ceiling was a replay-path artifact rather than a hard RP1 link limit.
- `2026-03-31`: Remote on `michael@totem1.local`: rebuilt `heart_pi5_scan_loop.ko` after adding `mmio_raw` and `mmio_unroll8` replay modes. With `mmio_wc=1`, unrolling the MMIO FIFO stores raised single-session sink throughput from `~336 MB/s` to `~555-556 MB/s` and raised real `64x64 x4`, `parallel=1`, `pwm_bits=11` resident refresh from `~915 Hz` to `~1514-1515 Hz`; raw stores were not materially better than unrolled `writel_relaxed()`, so the module now defaults to `mmio_unroll8=1` with `mmio_raw=0`.
- `2026-03-31`: Remote on `michael@totem1.local`: multi-session sink benchmarks with `mmio_replay=1`, `mmio_wc=1`, and `mmio_unroll8=1` reached about `556 MB/s` for `1` session, `1.07 GB/s` for `2`, `1.43 GB/s` for `3`, and `1.71 GB/s` for `4`, confirming the unrolled WC replay path scales well beyond the earlier per-channel ceiling.
- `2026-03-31`: Remote on `michael@totem1.local`: tested `mmio_cached_frame=1` to keep MMIO replay frames in cached kernel memory. Under `mmio_wc=1` this produced implausibly high apparent refresh (`>5 GB/s` sink throughput), and neither `wmb()`, `pio_sm_tx_fifo_level()`, nor experimental FIFO-alias readback (`mmio_fifo_read_flush=1`) made the completion accounting trustworthy. The working conclusion is that WC-mapped posted writes are not ordered with the current drain/count path, so `mmio_wc=1` is left experimental and the honest module default is back to non-WC MMIO replay (`mmio_wc=0`, `mmio_cached_frame=0`).
- `2026-03-31`: Remote on `michael@totem1.local`: revalidated the honest non-WC MMIO replay path after the WC flush experiments. `64x64 x4`, `parallel=1`, `pwm_bits=11` measured about `157.99 MB/s` sink throughput and about `429.9 Hz` steady resident refresh, matching the direct non-WC FIFO MMIO measurements and confirming the earlier `~546-556 MB/s` WC numbers were measurement artifacts rather than true device-paced refresh.
- `2026-03-31`: Remote on `michael@totem1.local`: swept non-WC MMIO write primitives (`scalar`, `writel_relaxed` unroll-8, `__raw_writel`, `__raw_writel` unroll-8, and `iowrite32_rep`). All converged to the same honest single-session sink throughput (`~157-158 MB/s`), which confirms the non-WC ceiling is the RP1 FIFO/device path rather than CPU loop overhead.
- `2026-03-31`: Remote on `michael@totem1.local`: size-swept the sink benchmark from `4 KiB` to `4 MiB` frames for both non-WC and WC coherent MMIO replay. Non-WC plateaued at `~157 MB/s`, while WC coherent climbed with frame size and stabilized at `~559 MB/s` for `1 MiB` and `4 MiB` frames. The WC coherent path also sustained `~1485 Hz` on real `64x64 x4`, `parallel=1`, `pwm_bits=11` scan over a `1 s` window. That makes `mmio_wc=1` with `mmio_cached_frame=0` the best supported default again; the invalid configuration is specifically cached-source WC replay.
- `2026-03-31`: Remote on `michael@totem1.local`: added a `mmio_writesl` replay mode and compared it with `iowrite32_rep()` and the unrolled `writel_relaxed()` WC coherent path. `writesl()` and `iowrite32_rep()` both dropped single-session sink throughput to `~330 MB/s`, while the unrolled `writel_relaxed()` path held `~545 MB/s`, so the unrolled relaxed-store path remains the best FIFO writer.
- `2026-03-31`: Remote on `michael@totem1.local`: rechecked multi-session sink throughput with the final WC coherent default (`mmio_wc=1`, `mmio_cached_frame=0`, `mmio_unroll8=1`). Aggregate throughput reached about `544.5 MB/s` for `1` session, `1.06 GB/s` for `2`, `1.41 GB/s` for `3`, and `1.70 GB/s` for `4`, confirming the higher single-session result scales cleanly.
- `2026-03-31`: Remote on `michael@totem1.local`: added `mmio_unroll16` and `mmio_prefetch` replay modes and compared them with the WC coherent `mmio_unroll8` default. The larger unroll and source prefetch did not materially change single-session sink throughput (`~546-547 MB/s`) or steady `64x64 x4` resident refresh (`~1486-1490 Hz`), so `mmio_unroll8=1` remains the default.
- `2026-03-31`: Remote on `michael@totem1.local`: tried the arm64 `__iowrite32_copy()` helper as a FIFO writer. That path hung `pi5_scan_bench` badly enough to require a Pi reboot, so the experimental `mmio_copy32` path was removed rather than left available as a footgun.
- `2026-03-31`: Remote on `michael@totem1.local`: after rebooting from the failed `mmio_copy32` experiment, a one-off run briefly reported about `661-670 MB/s` sink throughput and `~1821 Hz` steady `64x64 x4` refresh, but that spike did not survive a clean rebuild/reload of the module. The reproducible post-cleanup baseline returned to about `547 MB/s` single-session sink throughput and `~1490 Hz` steady `64x64 x4` resident refresh for the WC coherent `mmio_unroll8` path.
- `2026-03-31`: Remote on `michael@totem1.local`: exposed `clkdiv_mul` / `clkdiv_div`, `worker_cpu`, and `worker_fifo_low` as kernel module parameters. Sweeping the PIO clock divider from `2.0` down to `0.25` did not materially change sink throughput or steady `64x64 x4` replay on the WC coherent path, and pinning the replay worker to CPU 0 with `sched_set_fifo_low()` likewise left the steady ceiling unchanged at about `546-547 MB/s` / `~1490 Hz`. Those paths remain available for debugging, but they are not meaningful tuning levers for the current bottleneck.
- `2026-03-31`: Remote on `michael@totem1.local`: added `mmio_batch_replays` so the MMIO worker can replay the same resident frame multiple times before a single drain/completion sync, while still keeping the very first presentation unbatched. On `64x64 x4`, `parallel=1`, `pwm_bits=11`, `mmio_batch_replays=8` or higher improved steady resident refresh from about `1488 Hz` to about `1536 Hz` and raised single-session sink throughput from about `546.8 MB/s` to about `564.4 MB/s`. The gain is modest (`~3%`), but it confirms the drain/completion path still costs some throughput on unchanged-frame replay.
- `2026-03-31`: Remote on `michael@totem1.local`: `mmio_batch_replays` does not materially improve beyond `16`; `4` and `8` reached about `1528 Hz` / `561.5 MB/s`, while `16` and `32` plateaued at about `1536 Hz` / `564.4 MB/s`. The first-presentation latency stayed flat at about `0.73 ms`, but one-shot `display_hz` becomes intentionally less meaningful because later waits can complete in batched chunks rather than one presentation at a time.
- `2026-03-31`: Added all-black group compression to the Pi 5 scan format. Packed scan groups now emit a compact blank-run opcode when every shifted pixel word equals the group `blank_word`, and the kernel-loop / userspace PIO programs gained a matching blank-run branch. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench` and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`.
- `2026-03-31`: Remote on `michael@totem1.local`: with the blank-run format and `mmio_batch_replays=8`, `64x64 x4`, `parallel=1`, `pwm_bits=11` now scales with actual packed bytes instead of always paying the dense worst case. Dense synthetic frames shrank from `91,872` words to `67,392` words (`96` blank groups compressed) and steady resident refresh rose from about `1,536 Hz` to about `2,088 Hz`. Fully black frames shrank to `2,112` words (`352` blank groups compressed) and reached about `55.5 kHz`. Sparse three-pixel frames shrank to `7,467` words (`328` blank groups compressed) and reached about `16.3 kHz`. Those results line up with the existing `~560 MB/s` coherent-WC replay ceiling, confirming payload size is now the main lever.
- `2026-03-31`: Refined the blank-group compaction so fully blank groups are omitted from the packed stream entirely unless the whole frame is blank, in which case one minimal blank group is retained to keep the display dark. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba` and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`.
- `2026-03-31`: Remote on `michael@totem1.local`: after omitting blank groups entirely, `64x64 x4`, `parallel=1`, `pwm_bits=11`, `mmio_batch_replays=8` improved again. Dense synthetic frames dropped from `67,392` to `66,816` words and steady resident refresh rose slightly from about `2,088 Hz` to about `2,104 Hz`. Fully black frames dropped from `2,112` words to `6` words and reached about `332.6 kHz`. Sparse three-pixel frames dropped from `7,467` to `5,481` words and rose from about `16.3 kHz` to about `21.1 kHz`. This confirms that eliminating entire blank groups is a real additional win once the kernel loop accepts variable-length packed frames.
- `2026-03-31`: Added `mmio_batch_target_bytes` as an experimental kernel parameter for byte-targeted steady-state batching. Remote on `michael@totem1.local`, a `1 MiB` target did not help dense `64x64 x4` frames (`~2,082 Hz` versus `~2,104 Hz` with fixed `mmio_batch_replays=8`), but it did raise sparse three-pixel frames from about `21.1 kHz` to about `22.2 kHz` and sent the pathological all-black `6`-word frame to multi-megahertz territory. The conclusion is that byte-targeted batching is only worthwhile for extremely small resident payloads, so the Pi was left on the more balanced fixed `mmio_batch_replays=8` default.
- `2026-03-31`: Remote on `michael@totem1.local`: after the payload-compression work, the best fixed `mmio_batch_replays` value shifted upward. For compressed dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, steady resident refresh improved from about `2,019 Hz` at `batch=1` to about `2,104 Hz` at `batch=8`, then plateaued at about `2,112 Hz` for `batch=16` and above. For compressed sparse three-pixel frames, the same sweep climbed from about `14.8 kHz` at `batch=1` to about `22.3 kHz` at `batch=64`. The best practical fixed setting for normal work is now `mmio_batch_replays=16`, which slightly improves dense replay while still helping sparse payloads. The Pi was left loaded with `mmio_batch_replays=16`.
- `2026-03-31`: Added consecutive identical-bitplane merging to the Pi 5 scan packer so adjacent raw groups for the same row pair can collapse into one longer dwell when the shifted pixel payload is identical. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, and `python -m pytest tests/device/test_rgb_display_pi5_scan_benchmark.py` (skipped locally because `HEART_RUN_PI5_SCAN_TESTS` was unset).
- `2026-03-31`: Added a `solid` Pi 5 scan benchmark pattern to measure flat-color workloads where identical-bitplane merging should pay off. Remote on `michael@totem1.local` via `cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- ...`: `64x64 x4`, `parallel=1`, `pwm_bits=11`, `frame_pattern=dense` stayed at `66,816` words / `~1,995 Hz` with `0` merged groups; `frame_pattern=sparse` dropped to `4,698` words / `~21.2 kHz` with `0` merged groups; `frame_pattern=solid` dropped to `8,352` words with `224` merged groups and reached `~16.6 kHz`; `64x64 x1`, `frame_pattern=solid` dropped to `2,208` words with `224` merged groups and reached `~63.0 kHz`. This confirms the new merge path helps simple flat-color content, while random-like dense frames remain limited by blank-group compression alone.
- `2026-03-31`: Broadened identical-bitplane merging so any duplicate nonblank payload within a row pair coalesces, not just consecutive planes. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`. Remote on `michael@totem1.local` via `cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- ...`: with the updated seed-based `frame_pattern=solid`, `64x64 x4`, `parallel=1`, `pwm_bits=11` now packs to `41,760` words with `64` merged groups and replays at `~2,820 Hz`; `frame_pattern=sparse` improved further to `3,915` words with `6` merged groups and `~24.5 kHz`; `frame_pattern=dense` remained at `66,816` words with `0` merged groups and about `1,740 Hz`; `64x64 x1`, `frame_pattern=solid` now packs to `11,040` words with `64` merged groups and reaches `~8.77 kHz`. This confirms the generalized merge helps partially sparse and flat-color scenes, not just all-white frames.
- `2026-03-31`: Retuned the WC MMIO resident-loop batching after the broader payload-merge work. Remote on `michael@totem1.local`, repeated `64x64 x4`, `parallel=1`, `pwm_bits=11` sweeps showed a clear tradeoff: fixed batching (`mmio_batch_replays=16`) held dense frames at about `2,111.7 Hz` and flat-color `solid` frames at about `2,815.6 Hz`, while byte-targeted batching improved tiny sparse frames (`3,915` words) up to about `29.97 kHz`. The best compromise target is `3 MiB`: `mmio_batch_target_bytes=3145728` keeps dense replay effectively tied with fixed `16` (`~2,111.7 Hz`), improves `solid` slightly to about `2,819.6 Hz`, and still keeps sparse replay near `29.7 kHz`. The kernel default is now `3 * 1024 * 1024`, and the Pi was rebuilt/reloaded with that default (`/sys/module/heart_pi5_scan_loop/parameters/mmio_batch_target_bytes = 3145728`).
- `2026-03-31`: Switched the Pi 5 scan data run from one `pull` per column to a 28-bit autopull stream and packed the GPIO pin words densely in Rust while leaving control/count/dwell words as 32-bit pulls. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`. Remote on `michael@totem1.local`, after rebuilding both `heart_pi5_scan_loop.ko` and `pi5_scan_bench`, `64x64 x4`, `parallel=1`, `pwm_bits=11` measured `58,624` words / `~2,392 Hz` for `dense`, `3,435` words / `~28.1 kHz` for `sparse`, and `36,640` words / `~2,652 Hz` for the seed-based `solid` flat-color frame. `64x64 x1`, `dense` dropped to `15,616` words and reached `~7.40 kHz`. This is the first payload reduction in this line of work that improves even the dense case without relying on scene sparsity.
- `2026-03-31`: Retuned the WC MMIO replay batching again after the 28-bit packed-run format landed. On `64x64 x4`, `parallel=1`, `pwm_bits=11`, fixed `16` reached about `2,399.7 Hz` dense / `33.37 kHz` sparse / `3,199.6 Hz` solid, `target=2 MiB` reached about `2,399.7 Hz` dense / `34.03 kHz` sparse / `3,211.6 Hz` solid, `target=3 MiB` reached about `2,391.7 Hz` dense / `33.82 kHz` sparse / `3,195.6 Hz` solid, and `target=4 MiB` reached about `2,413.7 Hz` dense / `34.03 kHz` sparse / `3,219.6 Hz` solid. With the smaller packed transport, `4 MiB` is back to the best overall point, so the kernel default has been restored to `4 * 1024 * 1024` and the Pi was rebuilt/reloaded with `/sys/module/heart_pi5_scan_loop/parameters/mmio_batch_target_bytes = 4194304`.
- `2026-03-31`: Rebased the Pi 5 packed pin words from GPIO bits `0..27` to the actual used `5..27` span, which cuts every shifted pin word from `28` bits to `23` bits without changing semantics because the Adafruit bonnet scan pins all live at GPIO `>= 5`. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`. Remote on `michael@totem1.local`, after rebuilding both `heart_pi5_scan_loop.ko` and `pi5_scan_bench`, `64x64 x4`, `parallel=1`, `pwm_bits=11` dropped to `48,384` words / `~2,898 Hz` for `dense`, `2,835` words / `~38.2 kHz` for `sparse`, and `30,240` words / `~3,528 Hz` for the seed-based `solid` flat-color frame. `64x64 x1`, `dense` dropped to `13,056` words and reached `~9.76 kHz`. This is another structural payload win that helps every retained data run, not just specific color-plane distributions.
- `2026-03-31`: Retuned the WC MMIO replay batching again after the 23-bit rebasing landed. Repeated `64x64 x4`, `parallel=1`, `pwm_bits=11` runs on `michael@totem1.local` showed `target=2 MiB` is now clearly better than fixed `16`: fixed `16` held about `2,911.6 Hz` dense / `40.4 kHz` sparse / `3,871.5 Hz` solid, while `target=2 MiB` reached about `2,919.6 Hz` dense / `41.27 kHz` sparse / `3,891.5 Hz` solid. The kernel default is now `2 * 1024 * 1024`, and the Pi was rebuilt/reloaded with `/sys/module/heart_pi5_scan_loop/parameters/mmio_batch_target_bytes = 2097152`.
- `2026-03-31`: Added a prefix/suffix blank-span trim format for Pi 5 scan groups. Nonblank groups can now omit leading and trailing blank columns by emitting compact blank-span markers around the retained packed data run, while all-blank groups use a minimal `blank -> end -> latch/active/dwell` sequence. This required widening the Rust packer slot sizing and merge logic in `rust/heart_rgb_matrix_driver/src/runtime/pi5_scan.rs` and updating both PIO programs in `rust/heart_rgb_matrix_driver/native/pi5_pio_scan_shim.c` and `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/heart_pi5_scan_loop.c` to a 31-instruction parser. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`.
- `2026-03-31`: Remote on `michael@totem1.local`: after rebuilding `heart_pi5_scan_loop.ko` and rerunning `pi5_scan_bench`, the new prefix/suffix trim format kept `mmio_batch_target_bytes=2097152` as the best general setting. On a `200 ms` resident loop, `64x64 x4`, `parallel=1`, `pwm_bits=11` measured about `2,899.1 Hz` dense (`48,032` words), `3,848.8 Hz` solid (`30,560` words), and `685.9 kHz` sparse (`170` words). On a longer `500 ms` steady window with the same module settings, the same workload measured about `2,699.6 Hz` dense, `3,527.2 Hz` solid, and `632.7 kHz` sparse. Compared to the prior 23-bit packed-run format, this keeps dense/solid within a few percent while turning sparse frames into a dramatically smaller resident payload.
- `2026-03-31`: Extended the span-aware packer so it also splits large internal blank gaps inside a nonblank group (`INTERNAL_BLANK_RUN_MIN_PIXELS=5`) instead of only trimming prefix/suffix blanks. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`. Added a new Rust unit case that locks the striped single-panel compression result at `32 * 36` words.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the internal-blank splitter and adding a `striped` benchmark pattern, `mmio_batch_target_bytes=2097152` remained the right default. On a `200 ms` resident loop for `64x64 x4`, `parallel=1`, `pwm_bits=11`, the new format measured about `3,238.9 Hz` dense (`43,936` words), `3,848.7 Hz` solid (`30,560` words), `685.9 kHz` sparse (`170` words), and `5,773.2 Hz` striped (`20,160` words). On a longer `500 ms` steady window, dense held about `3,263.6 Hz` and striped about `5,837.2 Hz`. Compared to the prefix/suffix-only pass, this is a real dense-path win too: the synthetic dense frame dropped from `48,032` words / `~2.9 kHz` to `43,936` words / `~3.24 kHz`, while striped/text-like workloads benefit much more strongly.
- `2026-03-31`: Rechecked `mmio_batch_target_bytes` after the internal blank-gap split landed. Remote on `michael@totem1.local`, repeated `200 ms` sweeps for `64x64 x4`, `parallel=1`, `pwm_bits=11` showed dense replay is now essentially flat across the byte-targeted settings while fixed `16` is slightly worse: `target=0` held about `3,199 Hz`, `1 MiB` about `3,239 Hz`, `2 MiB` about `3,239 Hz`, and `4 MiB` about `3,239 Hz`. The new striped workload was similarly flat in the byte-targeted range at about `5.77-5.80 kHz`, with fixed `16` slightly lower at about `5.76 kHz`. The practical conclusion is that the packer change, not another kernel replay knob, was the real win, so the Pi remains on `mmio_batch_target_bytes=2097152`.
- `2026-03-31`: Gated `pyo3-stub-gen` behind a new `stubgen` Cargo feature and marked `src/bin/stub_gen.rs` with `required-features = ["stubgen"]`. This keeps Pi-side `pi5_scan_bench` builds from compiling the unused stub-generation parser stack, which had been crashing `cargo run --release --bin pi5_scan_bench` on `michael@totem1.local` inside `rustpython-parser`. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench` and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --features stubgen --bin stub_gen`.
- `2026-03-31`: Shortened the shared Pi 5 scan parser from 31 to 25 instructions by internalizing the trailer control path: `OE` now rides on sideset together with the clock, `LAT` is pulsed internally via `SET PINS`, and the packed trailer now carries only the end-of-spans marker plus dwell. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the 25-instruction parser, rebuilding `heart_pi5_scan_loop.ko`, and rerunning `pi5_scan_bench` with `batch_target_bytes=2097152`, dense `64x64 x4`, `parallel=1`, `pwm_bits=11` improved from the prior `43,936` words / `~3,239 Hz` path to `43,424` words / `3299.773 Hz` over a `1 s` steady window. The same tuned module measured `30,240` words / `3877.745 Hz` for `solid`, `19,840` words / `5917.619 Hz` for `striped`, and `140` words / `836558.697 Hz` for `sparse`. A follow-up sweep confirmed `batch_target_bytes=2097152` remains the best dense setting on the new format.
- `2026-03-31`: Tightened the Pi 5 resident-loop worker concurrency model: the worker now waits on a single `worker_runnable` flag with `wait_event()`, snapshots replay batches under the session lock, checks stop/disable state inside the batch loop, records replay timing/counter telemetry, binds the kthread to CPU 0, and runs it with `SCHED_FIFO` priority `80` via `sched_setattr_nocheck()`. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the worker/telemetry changes, rebuilding `heart_pi5_scan_loop.ko`, and reloading it with `batch_target_bytes=2097152 worker_cpu=0 worker_priority=80`, repeated dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000` runs stayed at about `3299.76-3299.79 Hz` steady resident refresh (`43,424` words) while one-shot `display_hz` improved to about `813-855 Hz`. The new kernel telemetry showed `resident_batches_submitted=276`, `resident_words_written=141,018,720`, `resident_drain_failures=0`, `resident_stop_requests_seen_during_batch=0`, `resident_mmio_write_ns≈995 ms`, `resident_drain_ns≈2.96 ms`, `resident_max_batch_replays=12`, `resident_worker_cpu=0`, and `resident_worker_priority=80`, confirming the steady-state bottleneck is still the MMIO burst rather than drain latency.
- `2026-03-31`: Replaced the old raw-or-blank span planner with an exact run-based planner that can emit repeat spans for any constant 23-bit GPIO word, not just blank columns. The packed group format is now `blank word -> zero or more raw/repeat spans -> zero terminator -> dwell`, and the shared Pi 5 parser widened to 27 instructions to decode the repeat opcode while still generating `LAT` and the active `OE` window internally. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the repeat-span transport, rebuilding `heart_pi5_scan_loop.ko`, and reloading it with `batch_target_bytes=2097152 worker_cpu=0 worker_priority=80`, dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000` dropped from `43,424` words / `~3.30 kHz` to `25,952` words / `6824.555 Hz`. A batch-target sweep confirmed `2097152` bytes remains the best dense setting (`0`: `6376.548 Hz`, `1 MiB`: `6803.530 Hz`, `2 MiB`: `6824.524 Hz`, `4 MiB`: `6799.539 Hz`, `8 MiB`: `6766.513 Hz`). At that tuned point the same path measured `800` words / `146864.255 Hz` for `solid`, `5600` words / `20980.546 Hz` for `striped`, and `125` words / `926658.460 Hz` for `sparse`.
- `2026-03-31`: Packed the repeat span length and repeated 23-bit GPIO word into one transport word instead of two. The shared Pi 5 parser shrank from 27 instructions to 24, blank groups became `blank word -> packed repeat control word -> terminator -> dwell`, and the single-panel Rust packer regression tests dropped from `160` to `128` words for the all-white case and from `352` to `224` for the striped case. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the packed repeat-word format, rebuilding `heart_pi5_scan_loop.ko`, and reloading it with `worker_cpu=0 worker_priority=80`, dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000` improved again from `25,952` words / `6824.555 Hz` to `23,968` words / `9414.361 Hz` at `batch_target_bytes=2097152`. A new batch-target sweep showed the best dense setting moved slightly upward: `0` => `8598.442 Hz`, `1 MiB` => `9400.385 Hz`, `2 MiB` => `9414.353 Hz`, `4 MiB` => `9449.344 Hz`, `8 MiB` => `9379.341 Hz`. The Pi now performs best with `batch_target_bytes=4194304` on this format.
- `2026-03-31`: Renamed the Pi 5 scan benchmark JSON fields so steady resident replay and distinct frame turnover are harder to confuse: `resident_steady_refresh_count` -> `resident_refresh_count`, `resident_steady_refresh_hz` -> `resident_refresh_hz`, `display_cycle_mean_ns` -> `distinct_frame_update_cycle_mean_ns`, and `display_hz` -> `distinct_frame_update_hz`. Compatibility aliases `sequential_cycle_mean_ns` and `sequential_cycle_hz` remain. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `make format`, and `make test`.
- `2026-03-31`: Inlined the first raw 23-bit GPIO word into the raw-span control word so both span types now carry their first/only pin word in the header. The shared Pi 5 parser widened from `24` to `25` instructions, dense single-panel packing for the benchmark seed dropped to `6208` words at `pwm_bits=11`, and the focused Rust regression suite now includes that dense raw-span case. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the inlined-raw-header format, rebuilding `heart_pi5_scan_loop.ko`, and benchmarking dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000`, the resident payload dropped from `23,968` words to `22,144` words. A fresh batch sweep on the new format gave `1 MiB` => `9468.366 Hz`, `2 MiB` => `9484.407 Hz`, `4 MiB` => `9519.356 Hz`, and `6 MiB` => `9449.394 Hz`, so `4 MiB` remains the best default. On that tuned point, `distinct_frame_update_hz` improved to about `297.972` and `resident_first_render_ns` stayed around `134369`.
- `2026-03-31`: Simplified the shared Pi 5 repeat-span parser so repeated pixels replay directly with `mov pins, x` instead of routing the repeated 23-bit word back through `OSR`. The packed transport format did not change; the point of the change was to keep the shared parser at `24` instructions while making the repeat path easier to audit in both `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/heart_pi5_scan_loop.c` and `rust/heart_rgb_matrix_driver/native/pi5_pio_scan_shim.c`. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the simplified `24`-instruction repeat parser, rebuilding `heart_pi5_scan_loop.ko`, and rebuilding `pi5_scan_bench` in release mode, dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000` stayed effectively flat at the current ceiling. A fresh dense batch sweep gave `1 MiB` => `9434.284 Hz`, `2 MiB` => `9484.369 Hz`, `4 MiB` => `9519.329 Hz`, and `6 MiB` => `9449.392 Hz`, so `batch_target_bytes=4194304` remains the best dense setting. A CPU-affinity sweep at that tuned point confirmed `worker_cpu=0` remains the right default (`CPU0`: `9519.322 Hz`, `CPU1`: `9519.304 Hz`, `CPU2`: `9519.316 Hz`, `CPU3`: `9449.356 Hz`). This parser simplification is worth keeping because it preserves the best measured dense result while making the shared parser smaller and easier to reason about, but it did not move dense `64x64 x4` materially past the existing `~9.52 kHz` steady resident refresh ceiling.
- `2026-03-31`: Remote on `michael@totem1.local`: for dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000`, the default Rayon-backed packer remained the best changed-frame tuning point. With `worker_cpu=0`, `worker_priority=80`, and `batch_target_bytes=4194304`, repeated release runs clustered around `distinct_frame_update_hz ≈ 289-297` with `stream_mean_ns ≈ 195-202 µs` and `resident_refresh_hz ≈ 9449-9519`. Forcing sequential packing with `HEART_PI5_SCAN_PACK_PARALLEL_THRESHOLD_WORDS=99999999` collapsed `distinct_frame_update_hz` to about `82`, `RAYON_NUM_THREADS=2` dropped it to about `160`, and `RAYON_NUM_THREADS=1` also landed around `82`, so the current parallel packer defaults should stay in place for dense changed-frame work.
- `2026-03-31`: Tried three Pi 5 changed-frame packer experiments aimed at raising `distinct_frame_update_hz`: per-thread scratch-vector reuse, a frame-local PWM expansion lookup table, and a linear run planner that traded exact span optimality for pack-time simplicity. Local validation for each pass was `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench` plus `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`; remote release benchmarks on `michael@totem1.local` did not show a repeatable win over the baseline, so those changes were reverted instead of being left as unproven complexity.
- `2026-03-31`: Hardened the Pi 5 resident-loop stop/load contract and error reporting. `heart_pi5_scan_loop.c` now tracks when the worker still owns a replay snapshot and makes `STOP` wait for quiescence before `LOAD` can replace the resident buffer; `pi5_scan.rs` now rejects `WiringProfile::AdafruitHat` for Pi 5 scanout v1 and propagates resident-loop `last_error` through `presentation_count()`. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_config_rejects_adafruit_hat_on_pi5`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_kernel_stats_surface_worker_errors_in_presentation_counts`, `make format`, and `make test`. Kernel-module compilation was not re-run locally because this host is macOS rather than a Linux Pi kernel build environment.
- `2026-03-31`: Updated `packages/heart-device-manager/src/heart_device_manager/install_rgb_matrix.sh` so the default Pi firmware setup applies `dtparam=audio=off`, `pcie_aspm=off`, and `isolcpus=3` idempotently instead of relying on one-off inline edits. Local validation: `bash -n packages/heart-device-manager/src/heart_device_manager/install_rgb_matrix.sh`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: added `pcie_aspm=off` to `/boot/firmware/cmdline.txt`, rebooted, and verified the live kernel command line in `/proc/cmdline`. After reboot, `modprobe heart_pi5_scan_loop` failed because the module was not installed under `/lib/modules` on that host, so the benchmark run used `insmod /home/michael/heart/rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/heart_pi5_scan_loop.ko` before rerunning `cargo run --release --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench -- --panel-rows 64 --panel-cols 64 --chain-length 4 --parallel 1 --pwm-bits 11 --iterations 1 --frame-count 8 --resident-loop-ms 200 --frame-pattern dense`, which reported `resident_refresh_hz ≈ 11546.262` and `distinct_frame_update_hz ≈ 455.001`.
- `2026-03-31`: Implemented the 4-state-machine Pi 5 scan transport rewrite that narrows the dense data stream to GPIO `5..23` and moves row address into three sticky control streams (`GPIO20`, `GPIO22`, `GPIO24..27`). Local validation: `make format`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the 4-state-machine address-holder rewrite, rebuilding `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/heart_pi5_scan_loop.ko`, rebuilding `pi5_scan_bench` in release mode, and running dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000` sweeps, the new transport was correct but slower than the previous single-data-stream path. The dense payload dropped to `19,649` transport words, but the best measured steady result was only about `5839.588 Hz` with `batch_target_bytes=4194304`; `2097152` reached about `5831.597 Hz`, and `1048576` about `5795.590 Hz`. `distinct_frame_update_hz` also regressed to about `270.686` at `4194304` versus the earlier single-stream baseline around `297.972`, so the extra four-FIFO MMIO fan-out currently costs more than the 19-bit payload reduction saves.
- `2026-03-31`: Reverted the experimental 4-state-machine `GPIO 5..23` Pi 5 scan transport back to the previous single-state-machine `GPIO 5..27` packed stream after benchmarking showed the extra FIFO/MMIO fan-out was a net regression. Reapplied the later safety fixes on top of the restored fast path: `Pi5ScanConfig`/Pi 5 backend still reject `WiringProfile::AdafruitHat`, `presentation_count()` now surfaces sticky resident-loop worker failures through `Pi5KernelResidentLoopStats::presentation_count_result()`, and `heart_pi5_scan_loop.c` still makes `STOP` wait for worker quiescence before `LOAD` can replace the resident frame. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_bench`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_pack_rgba`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: after syncing the single-state-machine rollback, rebuilding `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/heart_pi5_scan_loop.ko`, rebuilding `pi5_scan_bench` in release mode, and rerunning dense `64x64 x4`, `parallel=1`, `pwm_bits=11`, `resident_loop_ms=1000`, the faster baseline returned. `batch_target_bytes=2097152` measured `word_count=22145`, `resident_refresh_hz=9484.275`, and `distinct_frame_update_hz=284.216`; `batch_target_bytes=4194304` measured `word_count=22145`, `resident_refresh_hz=9449.329`, and `distinct_frame_update_hz=292.007`. Keep the single-stream transport as the baseline until a future multi-SM design proves a real Pi-side win on both steady refresh and changed-frame turnover.
- `2026-03-31`: Expanded maintainer-grade comments in the Pi 5 scan hot paths, focusing on the Rust span planner/packer (`write_scan_segment`, `plan_scan_segment_spans`, `emit_raw_run`, `pack_pin_words`, `compact_group_words`) and the kernel resident replay path (`heart_pi5_scan_loop_compute_batch_replays`, `heart_pi5_scan_loop_prepare_replay_batch_locked`, `heart_pi5_scan_loop_mmio_write_words`, and the inner worker replay loop). The added comments explain why the current structure exists, which throughput wins were empirical, and which invariants STOP/LOAD correctness depends on, without adding new code paths.
- `2026-03-31`: Expanded kernel-module documentation in `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/heart_pi5_scan_loop.c`, especially around session lifecycle flags, INIT/LOAD/START/STOP/WAIT/STATS semantics, resource-acquisition ordering in `heart_pi5_scan_loop_configure_locked()`, and why the PIO/output window and resident-buffer model are structured the way they are. This was a documentation-only pass intended to make the kernel path maintainable without rediscovering the benchmark history.
- `2026-03-31`: Updated `rust/heart_rgb_matrix_driver/kernel/pi5_scan_loop/dkms.conf` and `packages/heart-device-manager/src/heart_device_manager/install_pi5_scan_loop_dkms.sh` so the staged DKMS package version is derived from `rust/heart_rgb_matrix_driver/Cargo.toml` instead of being maintained as a second independent version source. Local validation: `bash -n packages/heart-device-manager/src/heart_device_manager/install_pi5_scan_loop_dkms.sh`, `make format`, and `make test`.
- `2026-03-31`: Added `scripts/hub75_color_cycle.py`, a small panel sanity test that exercises the `heart_rgb_matrix_driver` `RGBMatrix` compatibility path by cycling full-panel red, green, and blue fills across a configurable intensity sweep. Local validation: `./.venv/bin/python scripts/hub75_color_cycle.py --help`, `make format`, and `make test`.
- `2026-03-31`: Simplified `Pi5ScanFormat::Simple` into a deliberately slow explicit-word transport. In simple mode, Rust now expands every panel phase transition into a full 23-bit GPIO snapshot and the raw RP1 PIO program in `rust/heart_rgb_matrix_driver/native/pi5_pio_scan_shim.c` is reduced to `pull -> out pins -> jmp 0`, so `OUT PINS` is the sole owner of RGB, address, OE, LAT, and CLK during Pi 5 bring-up. The Pi 5 backend also skips the kernel resident-loop path when the simple format is selected so debugging stays on one transport.
- `2026-03-31`: Added and updated Pi 5 trace/audit tooling to match the explicit-word simple format. `pi5_scan_trace_diff` and `pi5_scan_pio_audit` now decode simple-mode groups with the active `Pi5ScanConfig`, and the focused regression `pi5_scan_trace_decoder_matches_expected_simple_group_trace` verifies the compacted simple buffer still round-trips to the expected GPIO-smoke semantics. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_trace_diff`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_trace_decoder_matches_expected_simple_group_trace`, `cargo run --quiet --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_trace_diff -- --format simple --frame-pattern solid --row-pair 0 --plane-index 0`, `make format`, and `make test`.
- `2026-03-31`: Remote on `michael@totem1.local`: synced the explicit-word simple Pi 5 transport, rebuilt `heart-rgb-matrix-driver` into `/home/michael/heart/.venv`, verified `cargo run --quiet --bin pi5_scan_trace_diff -- --format simple --frame-pattern solid --row-pair 0 --plane-index 0` still reported `traces match`, and reran `scripts/hub75_color_cycle.py --rows 64 --cols 64 --chain-length 1 --parallel 1 --iterations 1 --step-hold-seconds 2.0 --resubmit-interval-seconds 0.05 --intensities 64,255` on backend `pi5-adafruit-hat-pwm`. The script logged all six color steps and repeated submits, but the command still needed an external timeout to return, so panel-visible success still needs manual confirmation on hardware.
- `2026-03-31`: Added `scripts/hub75_single_line.py`, a one-line Pi 5 bring-up script that repeatedly submits a single horizontal line instead of a full-frame color cycle. The point is to reduce visual ambiguity: one stable line is a better signal than a whole panel fill when debugging scan timing or row-address mistakes. Local validation: `./.venv/bin/python scripts/hub75_single_line.py --help`.
- `2026-03-31`: Added `rust/heart_rgb_matrix_driver/bench/pi5_pio_smoke.rs`, a standalone raw Pi 5 PIO smoke binary that bypasses the matrix runtime worker and repeatedly submits one packed single-line frame through `Pi5PioScanTransport::submit_blocking()`. This path exists to answer the narrow question “can any RP1 PIO-driven frame stay visible?” without Python draw-loop or runtime-queue behavior in the middle. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_smoke`, `cargo run --quiet --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_smoke -- --help`, `make format`, and `make test`.
- `2026-03-31`: Extended `Pi5ScanTiming` with `simple_clock_hold_ticks` (env: `HEART_PI5_SCAN_SIMPLE_CLOCK_HOLD_TICKS`) so the explicit-word simple transport can hold each per-column `CLK=0` and `CLK=1` GPIO snapshot for multiple words. This is a bring-up-only visibility knob intended to make the RP1 waveform more comparable to the much slower direct GPIO smoke loop. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_trace_decoder_matches_expected_simple_group_trace`, and `cargo run --quiet --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_scan_trace_diff -- --format simple --frame-pattern solid --row-pair 0 --plane-index 0`.
- `2026-03-31`: Remote on `michael@totem1.local`: ran the standalone raw PIO smoke binary after syncing `pi5_pio_smoke.rs`, `pi5_scan.rs`, `tuning.rs`, and `pi5_pio_scan_shim.c`. `cargo run --quiet --bin pi5_pio_smoke -- --row-index 0 --line-thickness 1 --red 255 --green 255 --blue 255 --hold-seconds 6 --repeat-count 200` reported `backend=synchronous-simple`, `word_count=178528`, and `completed_submits=3`, confirming the direct synchronous raw PIO path can open, submit, and drain a single-line frame without the matrix runtime queue in the loop.
- `2026-03-31`: `make format`
- `2026-03-31`: `make test`
- `2026-03-31`: Added `build_simple_group_words_for_rgba()` plus the new `pi5_pio_hub75_probe` bin so Pi 5 full-frame bring-up can scan one row pair at a time with the already-working simple PIO transport instead of depending on the monolithic full-frame pack/replay path. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hub75_probe`.
- `2026-03-31`: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml pi5_scan_trace_decoder_matches_expected_simple_group_trace` still fails on macOS because bin-test linking pulls in the Pi-only `heart_pi5_pio_hello_shim` static library; use targeted `cargo check` locally and Pi-side release builds for the new PIO probe until bin-test linking is isolated.
- `2026-03-31`: Remote on `michael@totem1.local`: `~/.cargo/bin/cargo build --release --bin pi5_pio_hub75_probe`
- `2026-03-31`: Remote on `michael@totem1.local`: `HEART_PI5_SCAN_CLOCK_DIVIDER=1 HEART_PI5_SCAN_SIMPLE_CLOCK_HOLD_TICKS=1 HEART_PI5_SCAN_LSB_DWELL_TICKS=64 ./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hold-seconds 5 --frame-seconds 0.1 --pattern gradient` reported `completed_row_pair_scans=713`.
- `2026-03-31`: Remote on `michael@totem1.local`: `HEART_PI5_SCAN_CLOCK_DIVIDER=1 HEART_PI5_SCAN_SIMPLE_CLOCK_HOLD_TICKS=1 HEART_PI5_SCAN_LSB_DWELL_TICKS=1 ./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hold-seconds 5 --frame-seconds 0.1 --pattern checker` reported `completed_row_pair_scans=1066`, confirming the row-at-a-time adapted probe is still only about `6.7` full `64x64` frames/sec and therefore expected to show visible banding.
- `2026-03-31`: Added `row-address` mode to `pi5_pio_hub75_probe` so the Pi 5 bring-up path can test row selection independently of per-column image rendering. The mode lights exactly one row pair at a time with constant colors (`top=red`, `bottom=green`) and leaves all other row pairs blank.
- `2026-03-31`: Remote on `michael@totem1.local`: `HEART_PI5_SCAN_CLOCK_DIVIDER=1 HEART_PI5_SCAN_SIMPLE_CLOCK_HOLD_TICKS=1 HEART_PI5_SCAN_LSB_DWELL_TICKS=32 ./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hold-seconds 5 --frame-seconds 0.5 --pattern row-address` reported `completed_row_pair_scans=605`.
- `2026-03-31`: Added a pure-Rust simple-transport simulator in `rust/heart_rgb_matrix_driver/src/runtime/tests.rs` that reconstructs latched row-pair output from the explicit simple-format word stream. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_` and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_scan_trace_decoder_matches_expected_simple_group_trace`.
- `2026-03-31`: Added an opcode-level RP2040/RP1-style PIO simulator in `rust/heart_rgb_matrix_driver/src/runtime/pi5_pio_sim.rs` plus the `pi5_pio_sim_trace` bin so the simple Pi 5 HUB75 row engine can be stepped instruction-by-instruction against TX FIFO words without Pi hardware. The simulator models `pull`, `out`, `set`, optional side-set, and `jmp x--` / `jmp y--`, and it caught that the compact simple transport must encode shift-count and dwell-count as `count - 1` to avoid a phantom extra `pull` before latch. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_pio_emulator -- --nocapture`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_scan_trace_decoder_matches_expected_simple_group_trace`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_sim_trace`, and `cargo run --quiet --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_sim_trace -- --pattern blue --row-pair 0 --plane-index 0 --steps 30`.
- `2026-03-31`: Strengthened the opcode-level simple PIO emulator coverage with `pi5_simple_pio_emulator_outputs_column_words_without_osr_phase_drift`, which asserts that every `pc == 6` `OUT PINS, 23` step emits the exact source shift-word sequence in order. This proves the current simple path’s explicit `pull -> out pins, 23` loop wastes the high 9 OSR bits per FIFO word but does not misalign subsequent columns, because each new `pull` overwrites OSR before the next column. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_pio_emulator -- --nocapture`.
- `2026-03-31`: Added `pi5_simple_pio_emulator_matches_expected_gpio_values_across_row_phases`, which verifies the opcode-level simple PIO emulator drives the expected GPIO state in every major phase of one row slot: blank/address load, post-address settle, per-column clock low/high/recovery, latch high/low, active row load, and dwell hold. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_pio_emulator -- --nocapture`.
- `2026-03-31`: Added `pi5_simple_pio_emulator_drives_each_rgb_lane_as_expected`, which uses a 64-column lane-pattern frame to exercise `R1/G1/B1/R2/G2/B2` independently and asserts that every clock-low/high/recovery phase drives the expected RGB booleans for each column. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml` and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_pio_emulator -- --nocapture`.
- `2026-03-31`: Added `pi5_simple_pio_emulator_reconstructs_full_frame_position_by_position`, which runs the opcode-level simple PIO emulator across all 32 row pairs for a non-uniform 64x64 RGB position pattern and reconstructs every pixel position from the simulated `pc == 6` column outputs. This gives a full-panel per-position correctness check in software, not just row-pair or lane-local checks. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml` and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`.
- `2026-04-01`: Split the generic opcode-level PIO simulator into a dedicated Rust library crate at `rust/heart_pio_sim` and reduced `rust/heart_rgb_matrix_driver/src/runtime/pi5_pio_sim.rs` to the Pi 5 simple-HUB75 wrapper that builds the row-engine program and simulator config. Local validation: `cargo fmt --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_sim_trace`.
- `2026-04-01`: Added simple-transport guard tests that prove every row pair encodes the expected `A..E` address bits and that every simple `OUT PINS` word keeps `CLK` and `LAT` low so side-set/SET remain the only active owners of those control pins. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_group_words_ -- --nocapture`, `make format`, and `make test`.
- `2026-04-01`: Hardened the Pi 5 native and kernel C setup paths so the full `GPIO 5..27` OUT window is explicitly claimed as PIO-owned outputs and the Adafruit PWM bonnet bridge (`GPIO18` to legacy `GPIO4`) is quiesced back to inputs before reclaiming OE. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `make format`, and `make test`. Kernel-module compilation was not re-run locally because this host is macOS and does not provide `/lib/modules/.../build`.
- `2026-04-01`: Extended `pi5_pio_hub75_probe` with direct CLI timing knobs (`--clock-divider`, `--lsb-dwell-ticks`, `--post-addr-ticks`, `--latch-ticks`, `--post-latch-ticks`, `--simple-clock-hold-ticks`, `--pwm-bits`, and `--sys-clock-hz`) and made it print an effective timing estimate before running. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hub75_probe`, `cargo run --quiet --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hub75_probe -- --help`, `make format`, and `make test`. Remote validation on `michael@totem1.local`: `~/.cargo/bin/cargo build --release --bin pi5_pio_hub75_probe` and `./target/release/pi5_pio_hub75_probe --help`.
- `2026-04-01`: Widened the Pi 5 simple HUB75 bring-up transport from the synthetic `GPIO 5..27` bus to `GPIO 4..27` so `AdafruitHat` can drive OE on `GPIO4` while `AdafruitHatPwm` keeps `GPIO4` low by default. This updated the simple `.pio` program to `out pins, 24`, let the Pi 5 simple backend and bring-up scripts honor `--hardware-mapping`, and added guard tests for both `GPIO4` behaviors. Local validation: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, `make format`, and `make test`.
- `2026-04-01`: Replaced the Pi 5 simple row-engine format with a host-driven command stream that matches the working Piomatter model more closely: delay command + blank word, data command + literal shift words, then delay commands for latch-high, post-latch blank, and active dwell. The simple `.pio` program now interprets only data-vs-delay commands, `LAT` and `OE` ride directly in the streamed GPIO words, and the raw `/dev/pio0` shim keeps the SM running between submits instead of resetting it on every replay. Local validation: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `make format`, and `make test`.
- `2026-04-01`: Remote on `michael@totem1.local`: synced the new host-driven simple transport into `/home/michael/heart/rust/heart_rust`, rebuilt `~/.cargo/bin/cargo build --release --bin pi5_pio_hub75_probe`, and ran `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat --pattern blue-fill --hold-seconds 10 --frame-seconds 10 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`, which reported `backend=row-loop-simple`, `wiring=AdafruitHat`, `tick_ns=100.0`, `frame_hz=1055.743`, and `completed_row_pair_scans=11444`.
- `2026-04-01`: Updated the Pi 5 simple PIO simulator so it now models the native shim's submit-time lowering step and SM config instead of stopping at the rebased host word stream. `simulate_simple_hub75_group()` now expands literal GPIO words into the real `GPIO0..27` transfer window before simulation, and the simple simulator/C shims now consume side-set metadata from the generated `.pio` outputs rather than duplicating `sm_config_set_sideset()` literals by hand. This means the previous `sm_config_set_sideset(..., 2, true, false)` divergence is now represented as shared generated config instead of an independent C-only assumption. Local validation: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_pio_program_summaries_track_generated_program_shapes -- --nocapture`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `make format`, and `make test`.
- `2026-04-01`: Added repeat-command compaction to the Pi 5 simple host-driven transport so long constant column runs can use `repeat + one payload word` instead of `data + N literal payload words`. The Rust-side simple-group tests and helpers now parse command streams semantically instead of assuming fixed simple-word indices, which keeps the simulator and native-submit expansion checks valid when the simple protocol changes shape. Local validation: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_pio_program_summaries_track_generated_program_shapes -- --nocapture`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `make format`, and `make test`.
- `2026-04-01`: Fixed a repeat-path timing regression in the Pi 5 simple `.pio` program. The repeat loop now includes an extra low-cycle `nop` so it matches the literal-data loop's per-column clock cadence, and the runtime/native delay patching was retargeted to the actual repeat/data clock instructions instead of stale pre-refactor opcode indices. Local validation: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`, `cargo run --quiet --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_sim_trace -- --pattern blue --row-pair 0 --plane-index 0 --steps 30`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `make format`, and `make test`. Remote validation on `michael@totem1.local`: `~/.cargo/bin/cargo build --release --bin pi5_pio_hub75_probe`, `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat --pattern blue-fill --hold-seconds 10 --frame-seconds 10 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`, and `./target/release/pi5_pio_hub75_probe --panel-rows 64 --panel-cols 64 --chain-length 1 --parallel 1 --hardware-mapping adafruit-hat --pattern row-bars --fixed-row-pair 0 --hold-seconds 10 --frame-seconds 10 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`.
- `2026-04-01`: Made the Pi 5 simple repeat planner conservative after hardware showed visibly bold edge bars on mixed-repeat rows. Simple-mode repeat compression is now limited to uniformly repeated rows (for example full-blue fills), while mixed rows fall back to one literal data command so every column uses the same shift cadence. Local validation: `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_hub75_probe`, `make format`, and `make test`. Remote validation on `michael@totem1.local`: `~/.cargo/bin/cargo build --release --bin pi5_pio_hub75_probe` succeeded after syncing `pi5_scan.rs` and `pi5_pio_hub75_probe.rs`, but the current probe still failed both `--pattern row-bars --fixed-row-pair 0` and `--pattern blue-fill --fixed-row-pair 0` with `Pi 5 PIO transfer submission failed.`, so hardware confirmation of the conservative-repeat change is still blocked by the separate simple-submit transport failure in the probe path.
- `2026-04-01`: Added `PackedScanFrame::repeated()` plus a new `pi5_pio_checkpoint_demo` binary to compare three Pi 5 update checkpoints with a 200 ms red/green/blue full-screen cycle: `simple-single`, `simple-burst` (default `8` repeated copies of the same packed frame per submit), and `optimized-resident`. The demo intentionally splits transports: the raw `/dev/pio0` shim drives the simple checkpoints, while the optimized checkpoint uses `Pi5KernelResidentLoop` because the raw shim still only understands the simple host-driven stream. Local validation: `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_checkpoint_demo`, `cargo run --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --bin pi5_pio_checkpoint_demo -- --help`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_scan_packed_frame_repetition -- --nocapture`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`, `make format`, and `make test`. Remote validation on `michael@totem1.local`: `~/.cargo/bin/cargo build --release --bin pi5_pio_checkpoint_demo`, `./target/release/pi5_pio_checkpoint_demo --checkpoint simple-single --hardware-mapping adafruit-hat --hold-seconds-per-checkpoint 6 --color-interval-ms 200 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`, `./target/release/pi5_pio_checkpoint_demo --checkpoint simple-burst --hardware-mapping adafruit-hat --hold-seconds-per-checkpoint 6 --color-interval-ms 200 --burst-copies 8 --clock-divider 20 --simple-clock-hold-ticks 1 --lsb-dwell-ticks 64`, and `./target/release/pi5_pio_checkpoint_demo --checkpoint optimized-resident --optimized-hardware-mapping adafruit-hat-pwm --hold-seconds-per-checkpoint 2 --color-interval-ms 200 --pwm-bits 1 --lsb-dwell-ticks 64`, which failed fast with `Failed to open /dev/heart_pi5_scan_loop (errno=2).`
- `2026-04-01`: Added `scripts/piomatter_rgb_cycle.py` as the Piomatter parity baseline for full-screen RGB turnover on Pi 5. The script drives `adafruit_blinka_raspberry_pi5_piomatter.PioMatter` directly with `Geometry(width=64, height=64, n_addr_lines=5)`, `Colorspace.RGB888Packed`, and a selectable bonnet/HAT pinout, then cycles solid red/green/blue frames at a configurable interval. Local validation: `python3 -m py_compile scripts/piomatter_rgb_cycle.py`, `make format`, and `make test`. Remote validation on `michael@totem1.local`: `cd /home/michael/heart && PYTHONPATH=/home/michael/heart/src python3 scripts/piomatter_rgb_cycle.py --width 64 --height 64 --n-addr-lines 5 --pinout adafruit-matrix-bonnet --hold-seconds 3 --color-interval-seconds 0.2 --iterations 1`.
- `2026-04-01`: The Pi 5 simple raw `/dev/pio0` parity path can still fail with `Pi 5 PIO transfer submission failed (errno=110: Connection timed out)` even when the software-side Piomatter-style command stream and simulator are green. Keep the Rust/C simple transport work behind focused probes, but use the explicit Python Piomatter backend switch (`HEART_RGB_DISPLAY_BACKEND=piomatter`) as the stable in-tree simple path until the raw shim reaches live parity.
- `2026-04-01`: Reworked the simple Pi 5 transport model to match Piomatter more literally in software: the simple `.pio` program now uses one MSB-tagged data-vs-delay command stream with `autopull`, literal GPIO words now target real GPIO positions directly, and the shared `heart_pio_sim` crate now models `autopull` and shared SM config metadata so simulator and shim configuration cannot drift independently. Local validation: `./.venv/bin/python rust/heart_rgb_matrix_driver/tools/generate_pi5_pio_programs.py`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`, and `cargo check --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml`.
- `2026-04-01`: Added a Piomatter-backed simple runtime in `src/heart/device/rgb_display/piomatter_runtime.py` and an explicit backend selector in `src/heart/device/rgb_display/runtime.py`. Set `HEART_RGB_DISPLAY_BACKEND=piomatter` and optionally `HEART_PIOMATTER_PINOUT=adafruit-matrix-bonnet` to route Heart's `build_matrix_driver()` through Adafruit's known-good Pi 5 backend while preserving the same `submit_rgba()` and canvas API. Local validation: `python3 -m py_compile src/heart/device/rgb_display/piomatter_runtime.py src/heart/device/rgb_display/runtime.py`, `UV_CACHE_DIR=/Users/lampe/code/heart/.uv-cache .venv/bin/pytest tests/device/test_rgb_display_runtime.py`, `cargo test --manifest-path rust/heart_pio_sim/Cargo.toml`, and `cargo test --manifest-path rust/heart_rgb_matrix_driver/Cargo.toml --lib pi5_simple_ -- --nocapture`. Remote validation on `michael@totem1.local`: standalone parity check `PYTHONPATH=/home/michael/heart/src python3 scripts/piomatter_rgb_cycle.py --width 64 --height 64 --n-addr-lines 5 --pinout adafruit-matrix-bonnet --hold-seconds 3 --color-interval-seconds 0.2 --iterations 1`, plus Heart runtime parity `HEART_RGB_DISPLAY_BACKEND=piomatter HEART_PIOMATTER_PINOUT=adafruit-matrix-bonnet PYTHONPATH=/home/michael/heart/src python3 - <<'PY' ... build_matrix_driver(Rectangle.with_layout(columns=1, rows=1)) ... driver.submit_rgba(...) ... PY`, which cleanly cycled red/green/blue through Heart's own runtime API.
