# Heart Agent Instructions

## Import Style
- Never place imports inside functions or methods. Keep imports at module scope so dependencies are visible and stable.

## Testing Philosophy
- Do not introduce broad ad hoc test doubles such as `class _StubPeripheralManager:`. If code needs a manager in tests, prefer constructing a real `PeripheralManager` with explicit configuration, or extract a narrow protocol/helper API that can be tested directly with focused fakes.
- Keep test doubles scoped to the actual collaborator contract under test. Avoid stubbing large coordinator objects just to satisfy incidental attributes; that is a signal the production code needs a smaller dependency boundary.

## Recurring Bench Issues
- For RP1 HUB75 experiments, prefer well-parameterized candidates and runtime/config switches over cloning large assembly or runner blocks for each variant. Keep edits small and reusable so tuning changes do not require broad rewrites.
- No copy-paste forks for actual experiments; one-off prototypes are fine, but promoted experiments should share parameterized code.
- Prefer proving debugging and validation claims with well-formatted comparison tables that include the essential parameters, measurements, and verdicts.
- Label comparison-table metrics with direction hints such as "higher better" or "lower better" when the interpretation is not obvious.
- Saleae Logic2 capture can fail with `Cannot switch sessions while recording`. Before capture-based HUB75 validation, stop or close the active Logic2 recording session so connector-driven captures can start cleanly.
- Repo-local Saleae capture preflight can fail when the selected interpreter lacks the `saleae` package or no local Logic app is installed. Run preflight before trusting a dead CSV, and use whole-capture diagnostics to separate a silent export from a bad channel map.
- If a same-day Logic2 capture of a known-good PIO run and a slow manual GPIO toggle both export only two static rows, treat the problem as probe/analyzer routing or fixture grounding first. Do not spend more time tuning the kernel until a deliberate channel-identification test shows at least one observed edge.
- Before drawing totem-specific conclusions from Saleae captures, prove the probes are attached to the intended host. On 2026-05-12, captures stayed live while only `totem1` scanned and went flat when `totem1` was forced low, even with `totem4` actively running.
- For HUB75 HAT-style wiring, check both OE candidates: GPIO18 and legacy GPIO4. A static GPIO4 output-low is not equivalent to sequencing OE; mirror the blank/active OE waveform when validating the selftest or kernel worker path.
- For HUB75 logic-analyzer comparisons, always report OE active/blank duty alongside clock, latch, address, and color-channel checks. Treat active-low OE duty as a first-class brightness/performance signal.
- For the known-good totem3 blue RP1 HUB75 path, use self-contained Heart artifacts via `scripts/rp1_hub75_reproduce_totem_blue.sh` and `docs/RP1_HUB75_TOTEM_BLUE_REPRO.md`. Do not substitute the Rust color-loop runner when reproducing the 500 Hz state32 scanner.
- For regular P0/P1 chain2 state32 scanner tests, republish a fresh frame slot at `0xb800` before poking `START_MAGIC`; stale slab metadata can leave the worker stuck at `DMCP` with frame counter `0`.
- Do not use temporal PWM for HUB75 brightness experiments. Low-brightness temporal flicker looks bad and can create seizure-like flashing; debug color depth with true bitplanes or static non-temporal patterns instead.
- Before changing RP1 HUB75 shared-SRAM source offsets, check `docs/RP1_HUB75_SRAM_MAP.md`. Do not use `0x2000c000` or `0x20004000` as generic safe buffers; validate against payload size and reserved firmware/launcher/mailbox ranges first.
- When syncing this repo to a totem with `rsync`, exclude local-only workspaces and caches, including `.git/`, `.worktrees/`, `.uv-cache/`, `.venv/`, `.captures/`, `tmp/`, `target/`, `node_modules/`, and `__pycache__/`.
- Run expensive frame-tick-driven renderer state work through priority-aware background stream scheduling instead of inline on the main loop.

## Validation Log
- 2026-05-20: Tixyland now samples gamepad triggers for speed and ZL/ZR for hue without using d-pad, and Mandelbulb mirrors one OpenGL tile across any multi-panel layout including the Pi's 4x1 rectangle. Ran `uv run pytest tests/renderers/test_tixyland_renderer.py tests/navigation/test_multi_scene_lifecycle.py tests/renderers/test_mandelbulb_renderer.py tests/programs/test_lib_2026_configuration.py -q` and `uv run ruff check src/heart/renderers/tixyland src/heart/navigation/multi_scene.py src/heart/renderers/mandelbulb/renderer.py src/heart/programs/configurations/lib_2026.py tests/renderers/test_tixyland_renderer.py tests/navigation/test_multi_scene_lifecycle.py tests/renderers/test_mandelbulb_renderer.py tests/programs/test_lib_2026_configuration.py`.
- 2026-05-20: Multicolor renderer now warms its Numba pattern generator during renderer initialization/reload so Shroomed does not JIT-compile on its first visible frame. Ran `uv run pytest tests/renderers/test_multicolor_renderer.py -q` and `uv run ruff check src/heart/renderers/multicolor/renderer.py tests/renderers/test_multicolor_renderer.py`.
- 2026-05-19: 2026 library now adds the Mandelbulb mode immediately after Mandelbrot with a centered `mandel\nbulb` title. Ran `uv run pytest tests/programs/test_lib_2026_configuration.py -q` and `uv run ruff check src/heart/programs/configurations/lib_2026.py tests/programs/test_lib_2026_configuration.py`.
- 2026-05-19: Water cube trigger-held viscosity tuning now steps faster and continues scaling on each physics tick until it hits the configured bounds. Ran `uv run pytest tests/renderers/test_water_cube_provider.py -q` and `uv run ruff check src/heart/renderers/water_cube/provider.py tests/renderers/test_water_cube_provider.py`.
- 2026-05-19: Mandelbrot sampled trigger axes no longer synthesize ZL/ZR view-mode buttons, so triggers remain zoom-only while real bumper buttons still change view modes. Ran `uv run pytest tests/peripheral/test_input_core.py::TestMandelbrotControlProfile tests/renderers/test_mandelbrot_lifecycle.py -q` and `uv run ruff check src/heart/peripheral/core/input/profiles/mandelbrot.py tests/peripheral/test_input_core.py tests/renderers/test_mandelbrot_lifecycle.py`.
- 2026-05-19: Water cube controller input now samples the gamepad during physics updates; trigger axes tune accelerometer smoothing/viscosity and ZL/ZR shoulders rotate water hue, with the renderer using the state-selected HSV color. Ran `uv run pytest tests/renderers/test_water_cube_provider.py -q` and `uv run ruff check src/heart/renderers/water_cube/provider.py src/heart/renderers/water_cube/renderer.py src/heart/renderers/water_cube/state.py tests/renderers/test_water_cube_provider.py`.
- 2026-05-19: Mandelbrot keeps the renderer in `DeviceDisplayMode.FULL`, mirrors one base Mandelbrot panel across any multi-panel layout including the Pi's 4x1 rectangle, and preserves per-panel Julia rendering for Julia modes. Ran `uv run pytest tests/renderers/test_mandelbrot_lifecycle.py tests/peripheral/test_input_core.py::TestMandelbrotControlProfile -q` and `uv run ruff check src/heart/renderers/mandelbrot/scene.py tests/renderers/test_mandelbrot_lifecycle.py`.
- 2026-05-19: Accelerometer Manyfold detection now marks detected hardware accelerometers graph-owned so `PeripheralManager.start()` does not launch a second direct serial reader for the same KB2040. Ran `uv run pytest tests/peripheral/test_peripheral_configurations.py::TestManyfoldAccelerometerConfiguration tests/peripheral/test_peripheral_manager_graph.py::TestPeripheralManagerGraph::test_start_skips_graph_owned_peripherals -q` and `uv run ruff check src/heart/peripheral/configurations/__init__.py tests/peripheral/test_peripheral_configurations.py tests/peripheral/test_peripheral_manager_graph.py`.
- 2026-05-19: Mandelbrot renderer now defaults to `DeviceDisplayMode.FULL` again so cube layouts and shoulder-driven Mandelbrot/Julia view modes are available. Ran `uv run pytest tests/renderers/test_mandelbrot_lifecycle.py tests/peripheral/test_input_core.py::TestMandelbrotControlProfile -q` and `uv run ruff check src/heart/renderers/mandelbrot/scene.py tests/renderers/test_mandelbrot_lifecycle.py`.
- 2026-05-19: Mandelbrot sampled shoulder handling now treats trigger-axis presses as ZL/ZR view-mode commands so controllers reporting shoulders through axes still enter the alternate Mandelbrot/Julia modes. Ran `uv run pytest tests/renderers/test_mandelbrot_lifecycle.py tests/peripheral/test_input_core.py::TestMandelbrotControlProfile -q` and `uv run ruff check src/heart/peripheral/core/input/profiles/mandelbrot.py src/heart/renderers/mandelbrot/control_mappings.py tests/renderers/test_mandelbrot_lifecycle.py tests/peripheral/test_input_core.py`.
- 2026-05-19: Vibe's 2026 library title now uses the shared centered Kirby-colored title helper instead of its hardcoded white renderer title. Ran `uv run pytest tests/programs/test_lib_2026_configuration.py -q` and `uv run ruff check src/heart/programs/configurations/lib_2026.py tests/programs/test_lib_2026_configuration.py`.
- 2026-05-19: 3D fractal old imperative BUTTON_Y return-to-auto behavior is restored via sampled `GamepadButton.WEST`, while stale commented gamepad-control blocks were removed. Ran `uv run pytest tests/renderers/test_three_fractal_renderer.py -q` and `uv run ruff check src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`.
- 2026-05-19: Renderer gamepad controls now use direct `peripheral_manager.input_io.gamepad.sample()` reads instead of gamepad snapshot streams across 3D fractal, palette tunnel, Mandelbulb, Audio Storm, and existing Kirby/spritesheet controls. Ran `uv run pytest tests/renderers/test_palette_tunnel_renderer.py tests/renderers/test_three_fractal_renderer.py tests/renderers/test_mandelbulb_renderer.py tests/renderers/test_audio_storm_renderer.py tests/display/test_spritesheet_loop_renderer.py -q` and `uv run ruff check src/heart/renderers/palette_tunnel/renderer.py src/heart/renderers/three_fractal/renderer.py src/heart/renderers/mandelbulb/renderer.py src/heart/renderers/audio_storm/renderer.py src/heart/renderers/spritesheet/provider.py tests/renderers/test_palette_tunnel_renderer.py tests/renderers/test_three_fractal_renderer.py tests/renderers/test_mandelbulb_renderer.py tests/renderers/test_audio_storm_renderer.py tests/display/test_spritesheet_loop_renderer.py`.
- 2026-05-19: Palette tunnel now refreshes current gamepad snapshots before cursor movement so left-stick input works alongside stream-delivered keyboard/gamepad context. Ran `uv run pytest tests/renderers/test_palette_tunnel_renderer.py tests/renderers/test_three_fractal_renderer.py -q` and `uv run ruff check src/heart/renderers/palette_tunnel/renderer.py src/heart/renderers/three_fractal/renderer.py tests/renderers/test_palette_tunnel_renderer.py tests/renderers/test_three_fractal_renderer.py`.
- 2026-05-19: 3D fractal free-look now samples current gamepad state during controller processing so movement works even when stream snapshots are stale, and trigger controls normalize signed axes. Ran `uv run pytest tests/renderers/test_three_fractal_renderer.py -q` and `uv run ruff check src/heart/renderers/three_fractal/renderer.py tests/renderers/test_three_fractal_renderer.py`.
- 2026-05-19: Controller pairing panels now separate Bluetooth link status from app joystick-slot status and avoid overlapping status/input rows. Ran `uv run pytest tests/renderers/test_controller_pairing_renderer.py -q` and `uv run ruff check src/heart/renderers/controller_pairing/renderer.py tests/renderers/test_controller_pairing_renderer.py`.
- 2026-05-19: Kirby/spritesheet speed controls now normalize signed trigger axes and accept ZL/ZR shoulder buttons for slow/fast control. Ran `uv run pytest tests/display/test_spritesheet_loop_renderer.py -q` and `uv run ruff check src/heart/renderers/spritesheet/provider.py tests/display/test_spritesheet_loop_renderer.py`.
- 2026-05-19: Mandelbrot controls now edge-detect sampled gamepad shoulder/button commands so ZR still advances view mode if stream button-tap events are missed, and Mandelbrot title preview falls back to a black frame if preview warmup fails. Ran `uv run pytest tests/renderers/test_mandelbrot_title.py tests/renderers/test_mandelbrot_lifecycle.py tests/peripheral/test_input_core.py::TestMandelbrotControlProfile -q` and `uv run ruff check src/heart/peripheral/core/input/profiles/mandelbrot.py src/heart/renderers/mandelbrot/control_mappings.py src/heart/renderers/mandelbrot/title.py tests/renderers/test_mandelbrot_lifecycle.py tests/renderers/test_mandelbrot_title.py tests/peripheral/test_input_core.py`.
- 2026-05-19: Controller pairing mode now shows live per-slot gamepad diagnostics for d-pad, sticks, triggers, and held buttons. Ran `uv run pytest tests/renderers/test_controller_pairing_renderer.py -q` and `uv run ruff check src/heart/renderers/controller_pairing/renderer.py tests/renderers/test_controller_pairing_renderer.py`.
- 2026-05-19: Switch Manyfold detection now marks graph-owned real switch peripherals so `PeripheralManager.start()` does not launch a second legacy serial reader for the same Trinkey, while local fake switches still start normally. Ran `uv run pytest tests/peripheral/test_peripheral_manager_graph.py tests/peripheral/test_switch.py tests/peripheral/test_input_core.py tests/peripheral/test_peripheral_configurations.py -q` and `uv run ruff check src/heart/peripheral/core/manager.py src/heart/peripheral/configurations/__init__.py tests/peripheral/test_peripheral_manager_graph.py tests/peripheral/test_peripheral_configurations.py`.
- 2026-05-19: Accelerometer serial input now skips invalid UTF-8 bytes and logs missing/disconnected serial ports as reconnect warnings instead of traceback errors. Ran `uv run pytest tests/peripheral/test_accelerometer.py -q` and `uv run ruff check src/heart/peripheral/sensor.py tests/peripheral/test_accelerometer.py`.
- 2026-05-19: Multi-panel OpenGL renderer tiling fix for 3D fractal, palette tunnel, and spectrum/audio storm. Ran `uv run pytest tests/renderers/test_three_fractal_renderer.py tests/renderers/test_palette_tunnel_renderer.py tests/renderers/test_audio_storm_renderer.py -q` and `uv run ruff check src/heart/renderers/three_fractal/renderer.py src/heart/renderers/palette_tunnel/renderer.py src/heart/renderers/audio_storm/renderer.py tests/renderers/test_three_fractal_renderer.py tests/renderers/test_palette_tunnel_renderer.py tests/renderers/test_audio_storm_renderer.py`.
- 2026-05-19: Bouncing ball accelerometer provider restored and tuned with deadbanded force, capped force, and slower moving baseline adaptation. Ran `uv run pytest tests/renderers/test_bouncing_ball_renderer.py tests/programs/test_bouncing_ball_configuration.py -q` and `uv run ruff check src/heart/renderers/bouncing_ball src/heart/programs/configurations/lib_2026.py tests/renderers/test_bouncing_ball_renderer.py tests/programs/test_bouncing_ball_configuration.py`.
- 2026-05-19: Bouncing ball cube perspective changed to project distant ball positions and runway guide lines toward the left or right adjacent-wall precipice based on face-local ball position. Ran `uv run pytest tests/renderers/test_bouncing_ball_renderer.py tests/programs/test_bouncing_ball_configuration.py -q` and `uv run ruff check src/heart/renderers/bouncing_ball tests/renderers/test_bouncing_ball_renderer.py tests/programs/test_bouncing_ball_configuration.py`.
- 2026-05-19: Bouncing ball cube perspective now draws a projected adjacent-panel quadrilateral behind the runway lines. Ran `uv run pytest tests/renderers/test_bouncing_ball_renderer.py tests/programs/test_bouncing_ball_configuration.py -q` and `uv run ruff check src/heart/renderers/bouncing_ball tests/renderers/test_bouncing_ball_renderer.py tests/programs/test_bouncing_ball_configuration.py`.
- 2026-05-19: RP1 HUB75 runtime CIE1931 gamma mapping and totem brightness default update. Ran `cargo fmt && cargo test -q` from `rust/heart_rgb_matrix_driver` and `uv run pytest tests/test_rp1_hub75_regular_contract.py -q`.
- 2026-05-19: 2026 library centered title color now uses the Kirby default. Ran `uv run pytest tests/programs/test_lib_2026_configuration.py -q`.
